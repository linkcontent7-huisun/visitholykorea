# dependencies
node_modules/
.pnp/
.pnp.js

# build output
dist/
dev-dist/
build/
*.tsbuildinfo

# env & secrets
.env
.env.local
.env.*.local
!.env.example

# capacitor native projects (generated by `npx cap add`)
android/
ios/

# editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/

# logs
npm-debug.log*
yarn-error.log*
*.log

# test / coverage
coverage/
.vitest/

# deploy
# .vercel/project.json 은 배포 프로젝트 연결 정보라 커밋해 둔다(비밀 값이 아니다).
.vercel/*
!.vercel/project.json
!.vercel/README.txt
.netlify/

# supabase local
supabase/.branches/
supabase/.temp/

# 구버전 앱 사본·학습자료 — 저장소 밖 자료다. 코드는 GitHub 저장소가 기준이다 (ADR 0005).
supabase/visitholykorea_버전보관/
visitholykorea-학습자료/

# 일회성 진단 스크립트 — 한 번 돌려 보고 버리는 것이라 저장소에 남기지 않는다.
scripts/tmp-*
