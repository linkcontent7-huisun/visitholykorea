# dependencies
node_modules/
.pnp/
.pnp.js

# build output
dist/
dev-dist/
build/
*.tsbuildinfo

# env & secrets
.env
.env.local
.env.*.local
!.env.example

# capacitor native projects (generated by `npx cap add`)
android/
ios/

# editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/

# logs
npm-debug.log*
yarn-error.log*
*.log

# test / coverage
coverage/
.vitest/

# deploy
# .vercel/project.json 은 배포 프로젝트 연결 정보라 커밋해 둔다(비밀 값이 아니다).
.vercel/*
!.vercel/project.json
!.vercel/README.txt
.netlify/

# supabase local
supabase/.branches/
supabase/.temp/

# 구버전 앱 사본·학습자료 — 저장소 밖 자료다. 코드는 GitHub 저장소가 기준이다 (ADR 0005).
supabase/visitholykorea_버전보관/
visitholykorea-학습자료/

# 외부 원자료(공공데이터 등) — 참고용으로 로컬에 둔 것이지 앱 코드가 아니다.
# 용량도 크고(수십MB) 재배포 라이선스도 불확실해 저장소에 올리지 않는다.
2024년_외래관광객조사_원자료/

# 일회성 진단 스크립트 — 한 번 돌려 보고 버리는 것이라 저장소에 남기지 않는다.
scripts/tmp-*

# 사장님 개인 설계 문서 — 저장소에 올리지 않는다 (2026-09-07).
# 2026-09-07 에 실수로 공개 저장소에 올라가 히스토리까지 지운 적이 있다.
# 원본은 노트북 작업 폴더에 그대로 둔다.
visitholy작업설계도_*.txt
비지트홀리코리아-설계문서.pdf
# 저장소 루트의 PDF 전부 — 기획서·제안서 같은 개인 문서가 루트에 놓이는 일이
# 반복된다(2026-09-11 에 3개). 공유할 문서는 docs/ 아래에 둔다.
/*.pdf

# 창업 지원사업 신청 문서 — 공개 저장소에 올리지 않는다 (사장님 지시 2026-09-11).
# 노트북에는 그대로 둔다. 관광공모전·로드맵·기획 문서는 해당 없음.
docs/10-product/모두의창업-*
docs/10-product/모두의창업-이미지/
docs/10-product/창업지원-*
docs/10-product/*사업계획서*
docs/10-product/코딧세이-*
docs/10-product/린캔버스.md
docs/10-product/고객정의_역할지도_ICP.md
docs/10-product/2026-08-26-사업-비판검토.md
docs/10-product/금산_자문의뢰_기획서.md
