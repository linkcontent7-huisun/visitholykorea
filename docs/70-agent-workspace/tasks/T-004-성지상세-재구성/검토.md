# 검토 — T-004

## [1차] 2026-09-05 · 판정: 통과

### 완료 조건 대조

- [x] 스탬프 찍기 섹션을 시각적으로 구분한다 — 확인함(코드+화면).
      `src/pages/SiteDetailPage.tsx:527` `<section className="... rounded-[32px]
      border-2 border-brand-blue bg-brand-blue/[0.06] p-6 shadow-lg
      shadow-brand-blue/10">` 로, 다른 섹션의 "보라 세로줄 + h2" 스타일과 다르다.
      375px 로 명동대성당을 열어 실제로 파란 굵은 테두리 카드가 가장 먼저
      눈에 띄는 것을 스크린샷으로 확인했다. 위치도 그대로(성지 이야기 다음).

- [x] 방문 정보 5개를 접이식 그룹 하나로 묶는다 — 확인함(코드+화면+DOM 조작).
      `SiteDetailPage.tsx:732-776` 에 `<section aria-labelledby="visit-info-heading">`
      로 `VisitEtiquette`·`DirectionsCard`·`ContactCard`·`BarrierFreeCard`·
      `NearbyParishesCard` 5개를 감쌌다. 5개 컴포넌트 파일(`VisitEtiquette.tsx`,
      `DirectionsCard.tsx`, `ContactCard.tsx`, `BarrierFreeCard.tsx`,
      `NearbyParishesCard.tsx`)을 직접 열어 내부 코드가 이번 diff 에 없음을
      `git status`(수정 파일이 `SiteDetailPage.tsx`, `dictionary.ts` 딱 둘)로 확인.
      - 접힌 미리보기: 실제로 `[aria-controls="visit-info-panel"]` 버튼을 열기 전
        `document.querySelector('#visit-info-heading').nextElementSibling.textContent`
        로 읽은 값이 `"들어가기 전 안내 · 찾아가는 길 · 문의 · 주변 본당"` — 이
        성지는 무장애 정보가 없어서(TourAPI 403, 기존 주석대로 승인 대기) 빠졌다.
        더미 항목 없음을 확인.
      - 펼친 뒤 `#visit-info-panel` 안의 `h2/h3` 목록을 DOM 으로 뽑으니
        `["들어가기 전에", "찾아가는 길", "문의", "주변 본당·피정의집"]` 4개가 실제로
        나왔다(무장애 카드는 이 성지엔 데이터가 없어 원래도 안 그려지는 컴포넌트라
        정상). 미리보기 문구와 실제 렌더가 정확히 일치한다.
      - 기본 상태 접힘 — `aria-expanded` 초기값 `false` 확인.
      - `hasContact`(`site.phone/homepageUrl/fax`), `hasBarrierFree`
        (`barrierFreePlaces.length>0`), `hasNearbyParishes`
        (`nearbyParishes.length>0`) 판단 로직을 각 컴포넌트의 실제 return null
        조건과 대조했다 — 정확히 일치한다.

- [x] "기본 정보" 카드 주소 제거 — 확인함(코드+화면). `SiteDetailPage.tsx:361-368`
      에서 주소 칸이 빠지고 "교구 / 감성 태그" 한 칸만 남았다. 화면에서도
      "서울 · 감사"만 보이고 주소는 히어로 부제와 DirectionsCard 에만 남아있다.
      `DirectionsCard.tsx` 내부는 그대로(안 건드림).

- [x] 공유 버튼을 스탬프 섹션 안으로 이동 — 확인함(코드). `SiteDetailPage.tsx:574-584`
      `{stamped && (<button ... id="share-card-button">순례 스탬프 카드
      공유하기</button>)}` 가 스탬프 버튼과 같은 `<section>`(527번 줄) 안,
      스탬프 버튼 바로 아래에 있다. 예전 위치(NearbyParishesCard 뒤)에는
      더 이상 없음을 grep 으로 확인.

- [x] 도슨트·이어폰 가드·스탬프·순례자이야기 동작·조건 유지 — 확인함
      (코드+화면+DOM 조작, 근거는 아래 "추가로 실측한 것" 참고). 한 줄 남기기
      조건(`stamped && !myStamp?.note && !noteDismissed`)·공유 버튼 조건
      (`stamped &&`)은 코드 diff 상 바뀌지 않았다.

- [x] `npm run verify` 통과 — **직접 돌렸다.** 타입검사 0 에러, 린트 0 에러
      (`router.tsx` 기존 경고 1개는 무관), 테스트 39 파일 306개 전부 통과.

- [x] 모바일 375px 실측 4가지 — 아래 "추가로 실측한 것" 참고. 전부 실제로
      재현해서 확인했다(개발기록의 서술을 그대로 믿지 않고 직접 브라우저를
      띄워 재현함).

### 추가로 실측한 것 (개발기록을 그대로 믿지 않고 직접 재현)

- **도슨트 재생 중 방문정보 아코디언 개폐 → 재생 유지**: `npm run dev`(포트
  3100, dev-alt) 로 명동대성당을 열고, 데스크톱 크기(이어폰 가드가 걸리는
  좁은 화면·터치 포인터에서는 재생 자체가 막혀 있어 재생 여부를 확인할 수
  없음 — 이건 개발기록에도 이미 적혀 있고 나도 375px 에서 먼저 재현해서
  확인했다)에서 재생 버튼을 눌러 `speechSynthesis.speaking === true`,
  `aria-label === "멈춤"` 이 된 것을 확인한 뒤, `[aria-controls="visit-info-panel"]`
  버튼을 열고 닫는 동안 매 단계마다 `speechSynthesis.speaking` 을 찍어보니
  열기 전/연 직후/닫은 직후 모두 `true` 로 유지됐다. `useDocentPlayer`
  (`src/features/docent/hooks/use-docent-player.ts:189-196`)의 `useEffect`가
  `chapters` 참조가 바뀔 때 `speechSynthesis.cancel()` 을 부르는 코드임을
  직접 읽어 확인했고, `SiteDetailPage.tsx` 의 `docentChapters` 를 감싼
  `useMemo`(121-142번 줄) 덕에 `visitInfoOpen` 상태가 바뀌어도 참조가
  안 바뀌는 것을 코드로도, 실측으로도 확인했다. **개발기록이 "범위 밖처럼
  보이지만 완료 조건 때문에 고쳤다"고 밝힌 부분은 정직하고 정확했다** —
  `use-docent-player.ts`, `DocentPlayer.tsx` 등 도슨트 파일 자체는
  `git status` 상 전혀 수정되지 않았고, 수정은 오직 지시서의 수정 대상인
  `SiteDetailPage.tsx` 안에서만 일어났다. 범위 밖 위반 아님.

- **`npm run dev` 콘솔에 "docentChapters is not defined" 에러 재현 — 개발기록의
  진단은 부정확했지만, 실제 영향은 없음을 확인**: 개발기록은 이 에러를
  "HMR 편집 중간 상태에서 남은 과거 로그"로 판단했다. 이걸 그대로 믿지 않고
  dev-alt 서버를 **완전히 새로 재시작**(`preview_stop` 후 `preview_start`,
  캐시·HMR 상태 없는 새 프로세스)해서 명동대성당 페이지를 처음부터 열어봤는데,
  **재시작한 새 서버에서도 똑같이 재현됐다** — 즉 "편집 중 남은 과거 로그"가
  아니라 매 로드마다 나는 에러다. 다만 화면에는 어떤 시점에도 에러 화면이
  뜨지 않았고(로딩 스피너 → 정상 화면으로 바로 전환되는 것을 스크린샷으로
  확인), React 가 "error boundary 로 트리를 처음부터 다시 그린다"고 로그를
  남긴 뒤 스스로 복구했다. **`npm run build` 로 실제 배포에 쓰이는 프로덕션
  번들을 만들어 `npm run preview`(4173) 로 열어보니 이 에러가 전혀 없었다**
  — 즉 Vite 개발 서버 특유의 현상이고 실제 배포본(사장님과 심사위원이 보는
  화면)에는 영향이 없다. 완료 조건에 "콘솔 에러 없음"이 명시돼 있지 않고
  프로덕션에 영향이 없어 **수정 필요로 판정하지 않는다** — 다만 개발기록의
  "왜"는 틀렸다는 점, 그리고 dev 모드에서 뭔가 매 렌더 초기에 도는 로직이
  있다는 점은 "제안"에 남긴다.

### 고쳐야 할 것

없음.

### 제안 (범위 밖 — 통과 판정과 무관)

- `npm run dev` 로 명동대성당 상세 화면을 열면 매번(재시작한 새 서버에서도)
  콘솔에 `ReferenceError: docentChapters is not defined` 가 한 번씩 찍히고
  React 가 에러 경계로 복구한다. 화면에는 영향 없고 `npm run build` 프로덕션
  빌드에는 재현되지 않아 지금 당장 고칠 필요는 없지만, 원인(아마 Vite
  dev 서버의 모듈 사전 번들링·Fast Refresh 순서 문제로 추정 — 확인 못 함)을
  나중에 짚어두면 좋겠다.
- 개발기록이 제안한 대로 `tags` 배열도 매 렌더 새로 만들어지는데, 지금은
  문제를 안 일으켜 손대지 않는 것이 맞다.
- 방문 정보 아코디언 개폐에 애니메이션이 없다(즉시 나타남/사라짐) — 지시서
  완료 조건에 없으므로 이번 판정과 무관.

### 좋았던 것

- 완료 조건에 없는데도 "왜 이 수정이 범위 밖이 아닌지"를 개발기록에
  스스로 밝힌 점 — `useDocentPlayer` 내부를 고치는 대신 `SiteDetailPage.tsx`
  안에서 `useMemo` 로 참조를 고정한 선택이 정확했고, 근거(코드 줄·이유)가
  분명했다.
- 방문 정보 미리보기가 "그 성지에 실제로 있는 항목만" 나열되도록 각
  컴포넌트의 조건부 렌더링 로직과 정확히 같은 조건(`hasContact`,
  `hasBarrierFree`, `hasNearbyParishes`)을 그대로 재사용해, 더미 표시가
  구조적으로 불가능하게 만들었다.
- 6개 언어 번역 키를 빠짐없이 채웠고(`dictionary.test.ts` 통과로 재확인),
  기존 `directions` 키를 재사용해 불필요한 키 중복을 피했다.
