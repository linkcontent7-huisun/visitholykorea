# T-047 개발기록

## 시작 상태

- 2026-09-20, 별도 작업 공간 `fix/privacy-issue-47`에서 시작. `git status --short --branch`: `## fix/privacy-issue-47...origin/main`으로 깨끗함.
- 원래 작업 폴더에는 다른 작업의 미추적 파일이 있어 건드리지 않음.
- 이슈 #47은 GitHub에서 닫혀 있지만 코드의 미완성 문구는 남아 있었음.

## 결정 근거

- 사용자는 앱 안 계정 삭제와 `visitholykorea@gmail.com` 이메일 접수를 승인함.
- 운영 주체·개인정보 담당자·전화번호는 사용자 답변 대기. 확인 전에는 임의로 채우지 않음.
- 외부 업체의 처리 국가·리전은 확인되지 않아 추정하지 않음.
- 시행일은 최초 배포일로 소급하지 않고 수정 방침의 실제 배포일로 정함.

## 검증

- 시작 시 `npm run verify`: `figma-squircle` 미설치로 타입 검사 중단. 패키지 설치는 저장소 규칙상 하지 않음.
- 시작 시 `npm run test`: 52개 파일의 391개 테스트 통과, 1개 파일은 같은 패키지 누락으로 시작 못 함.
- 계정 삭제 정리·클라이언트 요청·순례 기록 사진 삭제 테스트는 실패를 먼저 확인한 뒤 구현했고 각각 통과함.
- `npm run lint`: 오류 0, 기존 `router.tsx`의 경고 2개.
- `npm run typecheck`: 현재는 위 패키지 누락 오류만 남음.

## 남은 일

- 개인정보 문의 창구는 `Visit Holy Korea 운영팀 / visitholykorea@gmail.com`으로 확정했다. 개인 이름·전화번호와 외부 처리 국가·리전은 공개하지 않는다.
- PR #85를 만들었다: https://github.com/linkcontent7-huisun/visitholykorea/pull/85
- 2026-09-20 22:34, 최신 `origin/main`을 병합하며 `docs/이어서-할-일.md`·`FaqPage.tsx`·`TermsPage.tsx` 충돌을 해결했다. 최신 main의 「간편 로그인」 용어와 이 작업의 실제 개인정보 처리 설명을 함께 유지했다.
- PR 자동 검증(CI·Vercel)이 진행 중이다. 완료 결과를 확인한다.
- 병합 뒤 Supabase Edge Function `delete-account`를 별도 배포해야 앱 내 계정 삭제가 실제로 작동한다. 운영 DB에서 실제 계정을 삭제해 시험하지는 않는다.
