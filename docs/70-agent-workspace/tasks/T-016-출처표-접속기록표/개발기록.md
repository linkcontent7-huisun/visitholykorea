# T-016 개발기록

작성: 2026-09-13

## 완료 조건 확인

| 완료 조건 | 확인 근거 | 결과 |
| --- | --- | --- |
| 자료 출처·접속 기록 표와 번역 현황 뷰 | `supabase/migrations/20260913100000_add_sources_events_coverage.sql`를 읽어 `site_sources`, `events`, `translation_coverage` 정의와 RLS를 확인했다. 표·정책은 재적용해도 안전하도록 `if not exists`, `drop policy if exists`, `on conflict do nothing`을 사용했다. | 완료 |
| 출처 시드와 최소 행 수 검증 | 서울 미사 자료 13곳, 최양업 자료 8곳을 지시서의 성지 이름 그대로 `holy_sites.name`에 join했다. 대표 사진 출처도 별도 insert-select로 넣고, 마이그레이션 끝 `do $$` 블록이 `site_sources` 전체 행 수 20 미만에서 예외를 내도록 확인했다. | 완료 |
| 접속 기록의 개인정보·권한 범위 | `events`에는 이름·이메일 없이 익명 `visitor_id`만 두고, anon·authenticated insert는 빈 ID와 타인 `user_id`를 RLS에서 막는다. 조회는 `public.admin_role() = 'admin'`만 가능하며 update/delete 정책은 만들지 않았다. | 완료 |
| 번역 현황 뷰 | `translation_coverage`는 `security_invoker = true`와 `coalesce(bool_or(...), false)`로 기존 원본 표의 읽기 권한을 따르고, 번역이 하나도 없는 성지도 false로 표시한다. | 완료 |
| TypeScript 행 타입 | `src/shared/types/database.ts`에 `SiteSourceRow`, `EventRow`, `TranslationCoverageRow`를 마이그레이션의 열·허용값과 맞춰 추가했다. | 완료 |
| 프로젝트 검증 | 2026-09-13에 `npm run verify`를 실행했다. typecheck·lint·test가 실행됐고 lint에는 기존 `src/app/routes/router.tsx` Fast Refresh 경고 2건만 있으며 오류는 없었다. | 통과 |

## 시드·정책 요약

| 대상 | 시드/권한 | 수량 또는 범위 |
| --- | --- | --- |
| `site_sources` | 서울 순례길 안내 책자 | 성지 13곳 |
| `site_sources` | 최양업 순례 여권 책자 | 성지 8곳 |
| `site_sources` | `image_source`가 있는 성지의 대표 사진 출처 | 적용 시점의 해당 성지 전체 |
| `site_sources` | 읽기 공개, 쓰기는 같은 교구를 편집할 수 있는 authenticated 사용자 | select / insert / update / delete |
| `events` | 익명·로그인 사용자 insert, admin만 select | insert / select |

## 지시자에게 남길 점

- DB 적용은 지시자 범위이므로 `npm run db:migrate`는 실행하지 않았다.
- 화면 코드는 변경하지 않았다.
