# 개발 기록 — T-001

## [1차] 2026-09-05

### 한 일

- `src/pages/HomePage.tsx` : 전면 재구성.
  - 헤더 아래에 **히어로 섹션** 신규 추가. `useSites({ limit: 100, withImageOnly: true })` 로
    사진 있는 성지만 후보로 받아, `dayIndex()`(하루 단위로 바뀌는 값) % 후보 수로 하나를
    고정 선택한다(하루마다 자동으로 바뀜, 별도 상태·DB 없음). `SiteThumbnail` 로 사진을
    채우고, `getDocentScript(heroSite.id)` 가 있을 때만 "오디오 가이드로 먼저 들어보기"
    알약 배지를 오버레이에 보여준다(없으면 표시 안 함 — 더미 금지).
  - 기존 `TodayQuietSection sites={allSites}` 호출에 `variant="compact"` 추가.
  - **순례 여권 미리보기**: `useSession()` 으로 `!session` 일 때만 노출. 헤더(제목+부제+
    가입 CTA)와 실측 `allSites.length` 로 만든 "0 / N" 줄을 새로 얹고, 본문은 기존
    `EmptyPassportPreview` 를 그대로 재사용(내부 수정 없음).
  - **순례자 이야기 미리보기**: `useSiteNotes(heroSite?.id)` 로 히어로 성지의 실제 방문
    한 줄을 조회, `note` 가 있는 첫 건(`storyNote`)이 있을 때만 카드 노출. 사진이 없으면
    (`photoUrl` null) 순례자 사진인 척하지 않고 `MessageCircle` 아이콘 자리로 대체.
    데이터가 하나도 없으면 섹션 자체를 렌더하지 않는다.
  - **보조 진입 칩 2개**: 기존에 화면 상단에 있던 큰 "붐빔피하기" 배너(Link+ArrowRight)를
    제거하고, `paths.alternatives`/`paths.compass` 로 가는 정사각형에 가까운 칩 2개로
    교체(`grid grid-cols-2`). AI 가이드 배너·`쉼표 순례길` 안의 기존 마음나침반
    안내(healing-quiz-cta) 등 다른 기능은 그대로 유지.
  - AI 가이드 배너는 칩 아래로 위치만 옮겼다(내용·동작 변경 없음).
  - 사용하지 않게 된 `ArrowRight` import 를 제거하고, `Headphones`·`MessageCircle` 을
    새로 import.
- `src/features/quiet/components/CrowdingBadge.tsx` : 배지 `<span>` 클래스에
  `shrink-0 whitespace-nowrap` 추가 — "아주 조용" 같은 라벨이 두 줄로 깨지던 버그 고침.
- `src/features/quiet/components/QuietSiteCard.tsx` : `compact?: boolean` prop 추가.
  `compact=true` 면 근거 문장(`reasons`) 목록 없이 이름 + 위치 한 줄 + 배지만 렌더하는
  축소 레이아웃을 반환한다. 기본값 `false` 로 기존 동작(성지 상세·다른 화면 영향 없음,
  실사용처가 HomePage 하나뿐임을 확인).
- `src/features/quiet/components/TodayQuietSection.tsx` : `variant?: 'full' | 'compact'`
  prop 추가(기본 `'full'`, 기존 호출부는 그대로 동작). `compact` 일 때:
  - 헤더를 날짜 라벨·부제 없이 "제목 + 더보기 링크"(→ `paths.alternatives`)로 축소.
  - 카드 목록에 `compact` 를 그대로 전달.
  - 대안 배너(`alternativesCta`)는 칩으로 옮겨갔으므로 반복 렌더하지 않음.
  - "추정값" 정직성 문구(`quietDisclaimer`)는 CLAUDE.md 의 정직성 원칙을 지키기 위해
    compact 에서도 남겨 두었다(간격만 좁힘) — 디자인시안-메모.md 에는 명시가 없어 developer
    재량으로 유지 쪽을 택함.
- `src/shared/i18n/dictionary.ts` : 6개 언어 신규 키 추가 — `heroDocentCta`,
  `quietSeeMore`, `crowdAvoidChip`, `passportSignupCta`, `passportPreviewSubtitle`,
  `pilgrimStoriesTitle`. 마음나침반 칩 라벨은 새 키를 만들지 않고 기존 `compassTitle`
  ("마음 나침반", 6개 언어 이미 존재)을 재사용했다.

### 확인한 것

- `npm run typecheck` : 통과 (에러 0)
- `npm run lint` : 통과 (기존에도 있던 `router.tsx` fast-refresh 경고 1건만 그대로 — 이번
  변경과 무관)
- `npm run test` : 통과, 39 파일 306개 테스트 전부 통과 (`TodayQuietSection.test.tsx` 의
  기존 두 케이스도 `variant` 미지정 시 `'full'` 기본값으로 그대로 통과함을 확인)
- 화면 확인: `npm run dev` 로 로컬 서버를 띄우고 브라우저 도구로 모바일 뷰포트
  375×812 에서 홈 화면을 실제로 열어 스크린샷으로 확인.
  - 히어로에 사진 있는 성지("합덕 성당", 대전·성당)가 실제로 뜸. 해당 날 후보에
    도슨트 원고가 없어 CTA 배지는 자동으로 숨었음(의도된 동작 — 다른 날/다른 후보에서는
    있는 성지가 뽑히면 노출됨을 코드로 확인함, `getDocentScript` 반환값에 조건부).
  - "고요 속으로" 목록이 이름+위치+배지만 나오는 한 줄 리스트로 축소돼 있고,
    "아주 조용 0" 배지가 줄바꿈 없이 한 줄로 나옴(버그 수정 확인).
  - 로그인 전 상태라 순례 여권 미리보기가 노출됨(제목/부제/가입 CTA/0-N/기존
    EmptyPassportPreview 내용 모두 정상).
  - 이 날 히어로 성지에는 방문 한 줄 데이터가 없어 순례자 이야기 섹션이 조용히
    숨음(완료 조건이 요구하는 동작 그대로).
  - 붐빔피하기/마음 나침반 칩 2개가 나란히 렌더됨.
  - AI 가이드 배너, 쉼표 순례길(감정 태그·코스), 탐방 그리드 모두 정상 렌더.
  - `document.documentElement.scrollWidth === window.innerWidth === 375` 로 가로
    스크롤 없음을 JS 로 직접 확인.
  - 큰 글자 모드(`data-text-size="lg"`) 켠 상태에서도 같은 375px 폭에서 가로 스크롤이
    생기지 않음을 스크린샷·JS 확인 둘 다로 검증.
  - 콘솔 에러 없음.

### 확인 못 한 것

- 사진 있는 성지 중 **도슨트 원고가 있는 곳**이 히어로로 뽑힌 날의 실제 화면은
  스크린샷으로 못 봤다(오늘 날짜 기준 후보가 마침 원고 없는 곳이었음). 코드 경로
  (`heroDocent &&` 조건부 렌더)는 `docent/data/scripts.test.ts` 등 기존 테스트로
  간접 검증되나, 두 상태(있음/없음) 모두를 스크린샷으로 대조하지는 못했다.
- 실기기(iOS/Android, Capacitor 빌드)에서는 확인 못 함 — 브라우저 뷰포트 에뮬레이션만
  확인.
- 다른 성지(순례자 한 줄이 실제로 존재하는 곳)가 히어로로 뽑혔을 때 "순례자 이야기"
  카드가 실제로 어떻게 보이는지는 스크린샷으로 못 봤다(오늘 후보에 해당 데이터 없음).
  코드상 조건은 확인함.

### 제안 (범위 밖이라 하지 않음)

- `EmptyPassportPreview.tsx` 와 `SiteDetailPage.tsx` 의 "순례자 이야기" 블록은 하드코딩된
  한국어 문구를 쓰고 있어(다국어 사전을 거치지 않음) 다른 화면 문구와 결이 다르다.
  이번 작업 범위(HomePage·CrowdingBadge)가 아니라 손대지 않았다.
- 히어로에 뽑히는 성지가 하루 단위로만 바뀌고 새로고침해도 그대로인데, 이게 기획
  의도와 맞는지("오늘 소개하는 성지"이므로 의도적으로 고정)는 확인했지만, 혹시 "매번
  들어올 때마다 다른 성지"를 원한다면 `dayIndex()` 대신 세션 단위 랜덤으로 바꿔야 한다 —
  지시서에 명시가 없어 "오늘 소개하는"이라는 문구를 그대로 따라 날짜 고정으로 구현했다.
