# T-034 — fr·pt·it 배포본 화면 전수 실측 (2026-09-21 09:30–11:00)

## 방법
agent-browser 로 `localStorage.vhk_language` 를 fr/pt/it 로 놓고 배포본(visitholykorea.com) 12개 화면을 열어 본문 텍스트를 읽고 스크린샷을 남겼다(`screenshots/`).
화면: home · map · search · routes · site(상세) · compass · records · menu · login · faq · terms · privacy.

## 결과
| 화면 | fr | pt | it | 남은 것 |
| --- | --- | --- | --- | --- |
| home · search · compass · records · menu · login · faq · terms · privacy | ✅ | ✅ | ✅ | 없음 |
| routes | ❌→✅ | ❌→✅ | ❌→✅ | 코스 11개 이름·부제·설명·경유지 메모가 영어였음 → **같은 날 4개 국어 번역해 DB 에 넣음(아래)** |
| site(상세) | ⚠️→✅ | ⚠️→✅ | ⚠️→✅ | 미사 시간 라벨·요일·구절, 사진 출처 기관명이 한국어였음 → **같은 날 화면에서 어휘 치환으로 고침**(b554096·1d96a19). 주소 한국어는 택시용 의도된 설계 |
| map | ⚠️ | ⚠️ | ⚠️ | 카테고리 라벨 한국어(보류 화면). 주소에 영어 괄호(addressRomanized)는 의도된 것 |

## 코스 번역 (같은 날 처리)
- 원본: `scripts/seed-route-translations-en.ts` 의 ROUTES(영어 11개, 경유지 메모 79개).
- 결과 파일: `data/translations/routes-{es,fr,pt,it}.json` — 각 11개 코스, 메모 79개. 연도·경유지 수를 영어와 대조해 일치 확인.
- 넣는 스크립트: `npx tsx scripts/seed-route-translations.ts --lang xx` (새로 만듦, `--reviewed` 붙이면 status=reviewed).
- DB 실측(9/21 11:00): `pilgrimage_route_translations` en/es/fr/it/pt 각 11행, `pilgrimage_route_site_translations` 각 79행.
- 배포본 `/routes` 에서 fr·pt·it 본문 확인함. 번역 상태는 `machine` — 사람 검수 전.

## 범위 밖 (하지 않음)
- 미사 시간 값 중 사전에 없는 고유명사(박물관 이름 등)는 한국어로 남음 — 지도 앱 표기와 같아 그대로 둠
- 지도 카테고리 라벨(보류 화면)
- `machine` 번역의 사람 검수
