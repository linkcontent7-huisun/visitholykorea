# T-014 개발기록

## 작업 전 상태

- `git status --short`에서 기존 미추적 파일 `.claude/settings.local.json`, `movie/`를 확인했다. 이 작업에서는 건드리지 않았다.

## 완료 조건 확인

| 항목 | 근거 |
| --- | --- |
| 마이그레이션 형식과 경유지 검증 | `supabase/migrations/20260912230000_add_seoul_and_choi_routes.sql`에 4개 코스 insert와 이름 join 방식의 경유지 insert를 작성했다. 끝의 `do $$` 블록이 코스별 경유지 수를 9, 9, 8, 21개로 검증하고, 다르면 예외를 발생시킨다. |
| 성지 이름 | 경유지와 update의 이름 목록은 `docs/70-agent-workspace/tasks/T-014-순례코스-서울3코스-최양업/지시서.md`의 표에서 그대로 복사했다. 별도 확인 스크립트는 만들지 않았다. |
| 코스별 경유지 수 | 서울순례길 1코스 9개, 2코스 9개, 3코스 8개, 최양업 신부의 길 21개로 SQL의 values와 검증 블록에 동일하게 반영했다. |
| update 문 | history 덧붙이기 8문, 미사 시간 description 덧붙이기 13문, 빈 전화번호 보완 13문을 작성했다. 모든 덧붙이기 update에는 표식 검사를 넣어 재실행 때 중복되지 않는다. |
| 검증 | `npm run verify`를 실행해 결과를 아래에 기록한다. 운영 DB 적용 명령은 실행하지 않았다. |

## 검증 결과

- 실행 예정: `npm run verify`
