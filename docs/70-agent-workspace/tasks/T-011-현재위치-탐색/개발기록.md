# T-011 현재 위치 기반 성지 탐색 개발기록

완료 2026-09-11 · 담당 Codex

## 구현 결과

- 메뉴에서 사용자가 직접 누를 때만 `navigator.geolocation.getCurrentPosition()`을 호출한다.
- 좌표는 `gpsLocation` React 상태로만 유지한다. localStorage·Supabase 요청·위치 이력 저장은 없다.
- 위치 요청 성공 시 홈, 탐색, 빈 여권 미리보기의 거리 계산이 GPS 좌표를 우선 사용한다.
- 메뉴에서 현재 위치를 해제하면 저장된 출발 지역 기준으로 즉시 돌아간다.
- 권한 거부, 브라우저 미지원, 기타 위치 오류는 각각 안내하고 기존 지역 선택을 막지 않는다.

## 확인

1. `git diff`로 GPS 좌표가 `SettingsProvider`의 React 상태에만 있고 저장·전송 호출이 없음을 확인했다.
2. 메뉴 코드에서 GPS 버튼만 `requestGpsLocation`을 호출해 자동 권한 요청이 없음을 확인했다.
3. 탐색·홈·여권에서 `gpsLocation ?? regionCoords(origin)` 우선순위를 확인했다.
4. `npm run verify`를 실행했다. 타입 검사 통과, 린트 오류 0건, 기존 Fast Refresh 경고 2건, 테스트 통과.

## 인수인계

- 실제 기기에서는 메뉴의 「현재 위치 사용」을 눌러 브라우저 권한 허용·거부를 각각 한 번 확인한다.
- 정밀 GPS는 배터리를 더 쓰므로 `enableHighAccuracy: false`를 유지했다. 현재 목적은 길 안내가 아니라 가까운 순 정렬이다.
- OS 또는 브라우저 설정에서 권한을 바꾸면 다시 「현재 위치 사용」을 눌러 새 좌표를 요청할 수 있다.

## 2026-09-20 설정 스위치·성지 찾기 보완

- 더보기의 「현재 위치 사용」을 `role="switch"` 스위치로 바꿨다. 켜짐은 GPS 좌표 사용, 꺼짐은 기존 출발 지역 기준이며, 위치 확인 중에는 중복 요청을 막기 위해 잠근다.
- 브라우저가 위치 기능을 지원하지 않으면 더보기 스위치를 비활성화하고, 권한 거부·일시 오류는 안내를 남긴 채 다시 시도할 수 있게 했다.
- 성지 찾기의 「내 위치로 검색」도 위치 확인 중 중복 요청을 막고, 미지원 상태에서는 비활성화했다. 거부·오류는 접근성 알림으로 표시하며, 위치를 얻지 못하면 GPS 반경 필터 없이 일반 검색 흐름을 유지한다.
- 확인: `npm run verify` 통과 — 타입 검사 통과, 린트 오류 0건(기존 Fast Refresh 경고 2건), 테스트 397개 통과(2026-09-20).

## 2026-09-20 홈 「오늘 방문하기 좋은 성지」 추천 규칙 보완

### 구현 결과

- 작업 시작 전에 `git status --short`를 확인했다. 다른 에이전트가 수정 중인 파일은 되돌리거나 정리하지 않았고, `HomePage.tsx`의 기존 주석 변경도 보존했다.
- 비로그인 사용자와 로그인했지만 GPS 좌표가 없는 사용자는 사진·연락처·좌표가 모두 있는 성지를 원문 이름순으로 정렬한 뒤, 한국 날짜 기준으로 다음 순위 4곳을 보여준다.
- 랜덤을 사용하지 않는다. 새로고침·홈 재진입·쿼리 재조회가 일어나도 같은 한국 날짜에는 같은 카드가 보인다.
- 로그인한 사용자에게 세션 메모리의 GPS 좌표가 있을 때만 현재 위치에서 가까운 순으로 4곳을 보여준다. 홈 진입 때 위치 권한을 새로 요청하지 않는다.
- 위치 권한 거부·오류·미지원·미요청 상태는 빈 추천으로 만들지 않고 날짜 순환으로 대체한다. GPS 좌표는 기존 정책대로 서버나 `localStorage`에 저장하지 않는다.

### 확인

- `src/features/sites/lib/nearest.ts`의 날짜 인덱스·순환 선택 테스트를 추가했다.
- `npm run verify` 실행 결과: 타입 검사 통과, 린트 오류 0건(기존 Fast Refresh 경고 2건), 테스트 403개 통과·1개 스킵(2026-09-20 17:46).
