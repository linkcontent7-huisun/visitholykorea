# ADR 0002. TourAPI 는 실시간 호출만 하고 저장하지 않는다

- 날짜: 2026-08-04
- 상태: 채택 (강제 사항)

## 맥락

2026 관광데이터 활용 공모전은 한국관광공사 OpenAPI(TourAPI) 활용이 필수 요건이고,
"실시간 활용"을 핵심으로 본다. 응답을 DB에 저장해두고 재사용하는 방식은 실격 위험이 있다.

동시에 우리 앱에는 성능·오프라인 요구도 있다. 산간 성지가 많아 통신이 불안정하다.
두 요구가 충돌하는 것처럼 보이지만, 실은 **데이터 출처가 다르다**.

## 결정

데이터를 두 갈래로 명확히 나눈다.

|            | 우리 데이터                             | 남의 데이터              |
| ---------- | --------------------------------------- | ------------------------ |
| 무엇       | `holy_sites` 195곳 (직접 수집·큐레이션) | TourAPI 주변 관광지·축제 |
| 저장       | 자유롭게 저장·캐싱                      | **금지**                 |
| 서비스워커 | 런타임 캐시 대상                        | 캐시 대상에서 제외       |
| 코드 위치  | `features/*/api/*.repository.ts`        | `shared/api/tour-api.ts` |

`shared/api/tour-api.ts` 의 함수들은 결과를 어디에도 저장하지 않는다.
`vite.config.ts` 의 workbox `runtimeCaching` 도 Supabase `holy_sites` 요청만 대상으로 한다.

## 아직 정리하지 못한 것 → **2026-08-28 해소됨**

전에는 `holy_sites.image_url` 중 일부가 `tong.visitkorea.or.kr` (한국관광공사) 주소여서
TourAPI 유래 데이터를 저장한 셈이라는 문제가 남아 있었다.

**8/28 DB 실측 결과 관광공사 주소는 한 건도 남아 있지 않다.** 저장된 47장의 출처는
`upload.wikimedia.org`(41장)와 `www.khs.go.kr`(국가유산청, 6장)뿐이다.
둘 다 TourAPI 와 무관한 경로이고, 각각 CC 라이선스·공공누리 제1유형으로 출처를 표기한다.

덧붙여, "상세 페이지에서 TourAPI 이미지를 실시간 조회한다"(옛 2번 선택지)는
**실효성이 없음을 8/28 에 확인했다.** 사진 없는 162곳을 TourAPI 좌표 검색으로 훑었더니
이름이 통하는 이미지가 7건뿐이었고 그중 유효한 것은 1건이었다(나머지는 온천·전통시장·향교).
성지는 TourAPI 관광지 목록에 거의 등재돼 있지 않다. 사진은 직접 촬영·교구 협조로 채운다.

## 파생 효과

TourAPI 를 상세 페이지 195곳 + 축제 정보까지 확장하면 호출량이 크게 는다.
인증키 일일 한도를 먼저 확인하고, 필요하면 "짧은 TTL 서버 프록시 + 레이트 리밋" 정도는
실시간성을 해치지 않는 선에서 검토한다.

## 2026-09-16 보충 — 관광공사 사진은 「호출값」만 저장한다

8/28 의 "실시간 조회는 실효성이 없다"는 판단은 **좌표 검색**에 대한 것이었다. 9/15 에 이름으로
관광사진 API·관광정보 API 를 훑자 사진 없는 155곳 중 34곳이 나왔고, 9/16 에 다음 방식으로 연결했다.

- `holy_sites.tour_photo_source / tour_photo_id / tour_photo_title` 에 **TourAPI 식별자**만 둔다
  (관광사진 galContentId · 관광정보 contentid). 이미지 파일도, 이미지 주소도 저장하지 않는다.
- 화면은 `/api/tour` 로 매번 받는다 (`getTourPhoto` · `useTourPhoto`). TanStack Query 메모리
  캐시(1시간, 새로고침하면 사라짐)만 거치고 서비스워커 캐시 대상이 아니다.
- 사장님 결정(2026-09-16): "open api 로 획득하는 사진은 이미지 자체를 DB 에 등록하는 것이 아닌,
  호출값을 저장하는 것" — 공공누리 저작물로 내려받아 재호스팅하는 안(9/15 검토)은 쓰지 않는다.
- 출처 표시: 상세 화면에 「사진 한국관광공사 ○○○ · 공공누리 제N유형」. 유형은 응답의 `cpyrhtDivCd`.
- 비용: 카드·목록에서 성지당 호출 1회가 늘어난다(메모리 캐시로 화면 간 중복은 없음). 개발계정
  한도(1,000건/일)에서는 부담이 되므로 운영계정 전환 결과를 확인해야 한다(`docs/이어서-할-일.md`).
