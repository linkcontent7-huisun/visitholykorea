# 이어서 할 일

마지막 작업: **2026-08-07** (인터넷 끊김으로 중단)

다음 세션에서 이 문서부터 읽으면 어디서 끊겼는지 알 수 있다.
끝난 일은 지우고, 새로 생긴 일은 여기 적는다.

---

## 🔴 가장 먼저 — 인터넷 되면 바로

```bash
git push origin main
```

커밋 2개가 로컬에만 있다. 푸시가 안 된 상태다.

---

## 지금까지 끝난 것

| | 내용 |
| --- | --- |
| ✅ | **교구명 SQL 수정** — 13곳이 `대구대교구` → `대구` 로 정상화. 교구 필터에서 사라졌던 성지들이 복구됐다. `군종`도 살아났다 |
| ✅ | 약현성당 분류 → `성당`, 절두산 감성태그 → `치유` |
| ✅ | **service_role 키 설정** — `.env.local` 에 `SUPABASE_SERVICE_ROLE_KEY` 추가됨. 이제 스크립트로 DB 쓰기 가능 (git 추적 안 됨, 안전) |
| ✅ | **대체 추천 기능** — "붐비는 관광지 → 조용한 성지". 코드·테스트 17개 완료 |
| ✅ | **SNS 레퍼런스 조사** — 계정 지도, 해시태그 실측, WYD 2027 확인 |
| ✅ | 해시태그 오류 수정 — `#성지순례` 는 가톨릭 태그가 아니었다 |

`npm run verify` 통과 (테스트 102개).

---

## 다음에 할 일 — 우선순위 순

### 1. 영어 번역 206곳 ← 여기서 끊겼다

**대상**: 206곳 / 51,899자 / 성지당 평균 252자
**넣을 곳**: `holy_site_translations` 테이블 (`site_id`, `language='en'`, `name`, `description`, `history`, `address_romanized`, `translation_status`)

**결정해야 할 것 — 사용자 답변 대기 중**

| 방식 | 장점 | 단점 |
| --- | --- | --- |
| **Claude 가 직접 번역** | 종교 용어 정확도 높음. 순교사 맥락을 이해하고 옮김 | 206곳을 여러 차례 나눠 처리해야 함 (한 번에 안 됨) |
| **Gemini API 일괄** | 빠름 | 감수 필요. Gemini 키가 Supabase 시크릿에만 있어 로컬에 없음 |

`translation_status` 에 `machine` / `reviewed` 구분이 이미 있어 둘 다 수용된다.

**왜 급한가** — 아래 WYD 2027 참고. 해외 청년 20~30만 명이 온다.

**작업 시작 방법**: `scripts/` 에 임포트 스크립트를 새로 만든다.
`scripts/content-report.ts` 의 `loadEnvLocal()` 패턴을 그대로 쓰되,
`SUPABASE_SERVICE_ROLE_KEY` 로 클라이언트를 만들어야 쓰기가 된다
(`holy_site_translations` 는 읽기 정책만 있어 anon 으로는 못 쓴다).

### 2. WYD 2027 을 콘텐츠·발표에 반영

조사에서 확인된 **확정 사실**이다. 상세는 `docs/30-content-ops/SNS-레퍼런스-조사.md` 3장.

- 교구대회 **2027.7.29~8.2**, 서울 제외 **15개 교구**
- 본대회 2027.8.3~8.8, 서울
- 최대 80만 명, **해외 청년 20~30만 명**
- 수호성인에 **성 김대건 안드레아와 동료 순교자**
- 공식 일정에 **솔뫼성지**, **해미읍성** — 둘 다 우리 DB 에 있다

할 일:
- [ ] 인스타 편성표에 D-day 코너·수호성인 시리즈 추가
- [ ] 한·영 병기 캡션을 기본값으로
- [ ] 공유 유도 CTA 를 캡션 규격에 넣기 (현재 없음)
- [ ] 순례 여권(스탬프) 연계 콘텐츠
- [ ] **공모전 발표자료에 WYD 를 근거로 넣기** — 앱이 필요한 이유가 날짜·규모까지 확정돼 있다

### 3. 대체 추천 기능을 화면에 붙이기

로직·훅·기본 컴포넌트까지 만들어 뒀다. **아직 어느 페이지에도 연결 안 됨.**

- `useAttractionSearch(keyword)` → 관광지 후보 목록
- `useAlternatives(spot, sites)` → 붐빔 + 대체 성지

UI 디자인을 사용자가 고민 중이라 `AlternativesList.tsx` 는 최소 형태로만 뒀다.
디자인이 정해지면 스타일을 입히고 라우트에 연결한다
(`app/routes/paths.ts` 에 경로 추가 필요).

### 4. 촬영 — 최대 병목 (사용자 담당)

**208곳 중 사진 있는 곳 14곳 = 7%.** 서울 26곳 중에는 3곳뿐이다.
로드맵은 `docs/30-content-ops/인스타그램-서울-콘텐츠-플랜.md` 에 5회차 동선으로 정리돼 있다.

조사에서 발견 — **혼자 성지 사진 프로젝트를 하는 개인 계정이 있다**(24번째 성지 진행 중).
핸들은 확인 못 했다. 인스타에서 `천주교 성지 사진 프로젝트` 로 찾아 협업을 검토할 만하다.
(무단 사용 금지. 정식 연락 필요.)

### 5. `holy-pilgrimage-backend` 저장소 삭제 (사용자 담당)

백업은 `보관_구버전저장소/` 에 있다.
**Claude 는 못 한다** — 토큰이 `linkcontent7-huisun` 이고 소유자는 `nohhuisun` 이라 권한이 없다.
GitHub 웹에서 직접 삭제해야 한다.

---

## 확인 못 한 것 — 나중에 채울 것

- 인스타 계정들의 실제 참여 수치(좋아요·공유·저장). 스크래핑이 막혀 있어 눈으로 세야 한다
- 성지 사진 프로젝트 계정 핸들
- 본당 단위 계정 전수 조사 (표본만 봤다)
