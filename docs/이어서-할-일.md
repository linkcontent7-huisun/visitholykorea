# 이어서 할 일

마지막 확인: **2026-08-26** (로컬 세션에서 DB 직접 실측)

다음 세션에서 이 문서부터 읽는다. 끝난 일은 지우고, 새로 생긴 일은 여기 적는다.

> **이 문서를 다시 쓴 이유** — 이전 판은 2026-08-07 에서 멈춰 있었는데
> 그 뒤로 커밋이 20개 쌓였다. 그래서 "마이그레이션이 미적용이다",
> "대전 번역은 미착수다", "테스트는 102개다" 같은 문장이 전부 사실과 달라졌고,
> 그걸 믿고 작업을 시작하면 이미 끝난 일을 다시 하게 된다.
> **문서가 코드보다 뒤처지면 없느니만 못하다.** 작업이 끝날 때마다 여기를 고친다.

---

## 🔴 지금 최우선

**0. 서비스 롤 키가 상해 있다 — 다만 번역 작업은 이제 이것과 무관하다.**

`SUPABASE_SERVICE_ROLE_KEY` 가 `JWT issued at future` 로 거부된다.
노트북 시계는 정상이고(2026-08-26 08:40 UTC, 구글 서버와 초 단위까지 일치),
앱이 쓰는 anon 키도 멀쩡하다(REST 조회 HTTP 200 — **배포 사이트는 문제 없다**).
상한 건 그 키 하나뿐이다.

**번역 스크립트 3개는 `SUPABASE_DB_URL` 직결로 바꿨다** (`scripts/lib/db.ts`).
JWT 를 안 거치므로 키가 상해도 돈다 — 마이그레이션이 같은 사고에도 멀쩡히
돌았던 것과 같은 이유다. 그래서 아래 1·2번은 키 없이 이미 끝났다.

키는 여전히 고치는 게 맞다. `scripts/import-contacts.ts` 가 아직 이 키를 쓰고,
Edge Function 배포에도 필요하다. Supabase 대시보드 → Project Settings → API
→ `service_role` 키를 복사해 `.env.local` 의 그 줄만 바꾸면 된다.

1. ~~`npm run db:migrate`~~ — **2026-08-26 적용 완료.** 대기 3개
   (`20260819000000_add_stamp_note` · `20260819010000_create_favorites` ·
   `20260824000000_create_compass_responses`)를 넣고 PostgREST 스키마 캐시까지
   갱신했다. `db:status` 는 이제 "남은 마이그레이션이 없습니다".
   **배포판의 한 줄 기록·즐겨찾기·나침반 저장이 이제 실제로 저장된다.**
2. ~~번역 12개 파일 122곳 import~~ — **2026-08-26 완료. 194/208곳이 DB 에 있다.**
   **영어 모드가 이제 실제로 영어로 나온다.** 남은 14곳은 아래 1번.
3. **코딧세이 2ROUND 선제출** — 🔴 **D-5. 마감 8/31(월) 14:00.**
   선제출 후 수정 가능하므로 **오늘 내는 게 맞다.** 제출물·체크리스트는
   `docs/10-product/창업지원-사업계획서-초안.md` 맨 위에 정리해 뒀다.
   구글폼: <https://forms.gle/8A59D9c6HosKCcq88>
4. 사진 — **169곳이 비어 있다. 지금 남은 가장 큰 위험이다.** 아래 2번.
   ⚠️ **`wikimedia_candidates_2026-08-23.csv` 를 일괄로 넣지 말 것.**
   전에 이 자리에 "라이선스까지 확인된 후보 78건부터 소진하라"고 적혀 있었는데
   **틀린 지시였다.** 그 목록은 라이선스만 봤고 무엇을 찍은 사진인지는 안 봤다.
   자세한 것은 아래 2번.
5. ~~청주 7곳 · 원주 3 · 부산 3 · 수원 1 번역~~ — **2026-08-27 완료.
   208/208곳. 영어 번역이 전부 끝났다.**
6. **visitholykorea.com 연결** — 사용자가 도메인을 이미 보유 중이다(8/27 확인).
   Vercel → Settings → Domains 에 추가하고 구매처 DNS 에 안내대로 레코드를 넣는다.
   기존 `visitholykorea-app.vercel.app` 은 계속 살아 있어 배포된 링크가 깨지지 않는다.
7. 🔴 **김범우 유배지가 두 곳으로 적혀 있다 — 사람이 정해야 한다.**
   DB 에 김범우 토마스의 유배·선종지가 **두 개** 있고, 서로 아니라고 말한다.
   - `단양 김범우 순교 성지`(원주교구) — "다블뤼 주교는 유배지가 「충청도
     동쪽 끝의 단양」이라고 기록했다… 1787년 유배지에서 숨졌다"
   - `김범우 순교자 성지`(부산교구, 밀양) — "오랫동안 유배지는 충청도
     단양으로 알려져 왔지만, 후손과 연구자들의 노력으로 실제로는 이곳
     밀양이었음이 밝혀졌다"

   둘 다 화면에 그대로 나가면 앱이 스스로를 반박한다. 한 사람의 유배지가
   두 곳일 수는 없다. **교구에 확인하고 한쪽 원문을 고쳐야 한다.**
   번역은 원문을 그대로 옮겼으므로, 한국어를 고치면 영어도 같이 고쳐야 한다.
   (같은 성지의 소개글은 "2021년 선포", 역사는 "2026년 선포"로 연도도 어긋나 있다.)

## 지금 상태 — 2026-08-27 실측

| 항목            | 상태                                                              | 확인 방법                               |
| --------------- | ----------------------------------------------------------------- | --------------------------------------- |
| 타입검사 · 린트 | 통과                                                              | `npm run verify`                        |
| 테스트          | **188개 전부 통과** (18개 파일, 8/27)                                   | 〃                                      |
| 배포 사이트     | anon 키로 DB 조회 200 확인                                        | <https://visitholykorea-app.vercel.app> |
| 성지            | **208곳**                                                         | DB `count(*)`                           |
| 사진            | **39곳만 있음 (18.8%). 169곳 비어 있음**                          | DB `image_url` count                    |
| **실사용자**    | **가입자 1명(본인) · 스탬프 0 · 여행기 0 · 즐겨찾기 0**           | `npx tsx scripts/usage-check.ts`        |
| 영어 번역 파일  | 16개 파일 **208곳 전부** 작성 완료                                | `data/translations/`                    |
| 영어 번역 DB    | **208 / 208곳 반영 완료** (8/27). 감수(`reviewed`)는 9곳뿐         | `npm run translate:status`              |
| 마이그레이션    | 파일 14개 **전부 적용** (8/26 에 대기 3개 처리)                   | `npm run db:status`                     |
| 서비스 롤 키    | **깨짐** — `JWT issued at future`. anon 키·DB 직결·번역은 정상    | —                                       |
| TourAPI 키      | **신청·발급 완료.** 8/27 실호출 `resultCode 0000 / OK`. **등급 미확인** | 공공데이터포털 마이페이지 |

> **TourAPI 운영계정은 이미 신청·발급됐다** (사용자 확인 2026-08-20).
> 여러 문서가 "9월 초 신청 필요"로 남아 있었고, 8/20 인계가 정정을 지시했는데도
> 아무도 안 고쳐서 8/26·8/27 문서에 "미신청"이 다시 들어갔다. 8/27 에 전부 정정했다.
> **남은 것은 등급 확인 하나** — 개발계정(하루 1,000건)과 운영계정(100,000건)은
> API 응답이 같아 호출로는 구분되지 않는다. 1,000건이면 홈 1회 로드가 21건이므로
> **하루 45회 접속에서 멈춘다.**

> **실사용자 1명이 지금 가장 아픈 숫자다.** 배포는 됐는데 아무도 쓰지 않는다.
> 사업계획서가 내건 가설(붐빔이 문제인가 · 프로그램에 돈을 낼 것인가)이
> 하나도 검증되지 않았다는 뜻이다. 근거와 대응은
> `docs/10-product/2026-08-26-사업-비판검토.md`.

최신 커밋은 `git log --oneline -1` 로 본다. 여기 적어두면 반드시 낡는다.

### 8/24 이후 새로 들어온 것 — PR #8

로컬 세션 3개가 8/20~8/24 에 만들고 push 하지 않은 채 두었던 작업이다.
원격에 이미 들어간 것(나침반 기억·여행기 작성 폼)과 3-way 머지해서 살렸다.

- **출발지(시·도) 설정** — 17개 시·도와 중심 좌표를 `shared/lib/regions.ts` 로
  분리하고, 홈·퀴즈·메뉴가 같은 출발지를 쓴다. 홈이 "가까운 성지" 순으로 바뀐다
- **`/region/:region` 지역 랜딩** — 지자체에 건넬 링크. 17개 시·도 전부 동작
- 붐빔 대체 추천 **버그 수정** — 등급이 안 내려가는 곳을 1위로 올리던 문제
- 코스 페어링 **버그 수정** — 성지를 피해 성지로 보내던 문제
- 한국어 조사 처리(`shared/lib/korean.ts`) — "…기념성당**로**" 제거
- 기록 탭 비로그인 안내 화면
- `scripts/seed-rest-places.ts` — 쉼자리 시설층 채우기

### 8/07 이후 새로 들어온 것 (전 판이 모르는 것)

전 판이 작성된 뒤 커밋 20개로 추가된 기능들이다. 문서 어디에도 반영돼 있지 않았다.

8/08~8/13:

- 전국 개관 지도 — 방문 상태 3단계 핀
- 순례 여정을 시간이 지날수록 옅어지는 선으로 그리기
- 옛 Supabase 프로젝트에서 성지 연락처 구조 복구 (`contact` 컬럼들)
- 대전 27곳 영어 번역 (`data/translations/en-대전.todo.json`)
- 홈 화면 메시지 전환 — "고요 속으로", 치유 톤
- 사진 중심 순례 앱 UI 레퍼런스 연구 (`docs/20-architecture/UI-레퍼런스.md`)

8/18:

- 순례 코스 — 박해 사건·인물 축으로 성지를 잇는 길 (`/routes`)
- 소셜 로그인 4종(네이버·카카오·구글·페이스북), 이용약관, FAQ
- `profiles` 테이블, 미인증 이메일 로그인 오류 메시지 개선
- 충청권 성지 37곳 설명을 교구 원자료 기준으로 전면 재작성
- 이미지 출처 표기 · 본당 미사 시간 컬럼
- 위키미디어 공용 이미지 19장 추가
- AI 페르소나 '미카엘' — 상대에 맞춰 답을 조절하는 규칙 추가

---

## 다음에 할 일 — 우선순위 순

### 1. 번역 122곳을 DB 에 넣기 — 어디까지 들어갔는지 이제 안다

**측정했다 (2026-08-26, DB 직접 조회).** 더 이상 "확인 필요"가 아니다.

- 파일: 15개 교구 **194곳** 작성 완료
- DB: **72곳만 들어가 있다** — 서울 25 · 대전 27 · 대구 20
  (상태값은 `machine` 63 · `reviewed` 9)
- **남은 것: 12개 파일 122곳**

> **먼저 0번(서비스 롤 키)을 해결해야 한다.** `translate:import` 는
> `SUPABASE_SERVICE_ROLE_KEY` 로 붙는데 지금 그 키가 `JWT issued at future`
> 로 거부된다. 키를 고치기 전에는 import 가 한 건도 안 들어간다.

넣기는 빈 칸을 건드리지 않으므로 여러 번 돌려도 안전하다.
아래 12줄을 순서대로 돌리면 194곳이 전부 채워진다.

```bash
npm run translate:import -- data/translations/en-전주.todo.json    # 18곳
npm run translate:import -- data/translations/en-수원.todo.json    # 17곳
npm run translate:import -- data/translations/en-춘천.todo.json    # 16곳
npm run translate:import -- data/translations/en-의정부.todo.json  # 11곳
npm run translate:import -- data/translations/en-안동.todo.json    # 10곳
npm run translate:import -- data/translations/en-인천.todo.json    # 10곳
npm run translate:import -- data/translations/en-마산.todo.json    #  9곳
npm run translate:import -- data/translations/en-부산.todo.json    #  8곳
npm run translate:import -- data/translations/en-원주.todo.json    #  8곳
npm run translate:import -- data/translations/en-제주.todo.json    #  8곳
npm run translate:import -- data/translations/en-광주.todo.json    #  6곳
npm run translate:import -- data/translations/en-군종.todo.json    #  1곳
```

넣은 뒤 다시 재면 194곳이어야 한다. 208 - 194 = **14곳은 원문이 8/17~18 에
재작성되어 아직 번역 자체가 없다** (청주 7 · 원주 3 · 부산 3 · 수원 화성 1).

> **함정** — `select('*', { head: true })` 는 테이블이 없어도 통과한다.
> 테이블 존재 확인은 반드시 컬럼을 지정해서 조회해야 한다.

아래는 이미 끝난 서울·대전 참고용이다(다시 돌릴 필요 없음).

```bash
npm run translate:import -- data/translations/en-서울.todo.json
npm run translate:import -- data/translations/en-대전.todo.json
```

들어가 있으면 다음 교구로 넘어간다. **웹에서 가능한 번역은 전부 끝났다 — 194/208곳.**

남은 14곳은 원문이 8/17~18 에 재작성되어 로컬에서만 할 수 있다:
청주 7곳 전체 · 원주 3곳(배론·단양 김범우·남종삼 유택지) ·
부산 3곳(기장·울산 장대·죽림굴) · 수원 화성 순교성지.
로컬에서 `npm run translate:export -- --diocese 청주` 식으로 최신 원문을 받아 번역한다.

> **⚠️ 스냅샷으로 번역하면 안 되는 곳 (원문이 8/17~18 에 수정됨)** —
> 청주 7곳 전체, 원주 3곳(배론·단양 김범우·남종삼 유택지),
> 부산 3곳(기장·울산 장대·울산 죽림굴), 수원 화성 순교성지.
> 이들은 로컬에서 `npm run translate:export` 로 최신 원문을 받아 번역한다.
>
> **⚠️ 이미 한 번역 중 원문이 나중에 바뀐 곳** — 대전 25곳(8/18 재작성)과
> 서울 절두산. 영어 번역(8/07·8/09 원문 기준)은 여전히 쓸 수 있지만 현재
> 한국어 설명과 내용이 다를 수 있다. 감수 때 대조할 것.

> **대구·전주 번역의 원문 출처** — 웹 세션은 DB 를 못 보므로 8/10 스냅샷
> (`data/archive/2026-08-supabase/snapshot_kaahuo_2026-08-10.json`)의 원문으로
> 번역했다. 8/10 이후 내용 수정 백업에 대구·전주가 없음을 확인했으니 원문은
> 최신이지만, import 전에 `source` 와 DB 값이 다르지 않은지 한 번 훑어보면 좋다.

교구별 실측 (2026-08-26). `DB` 열이 실제로 `holy_site_translations` 에 들어간 수다.

| 교구   | 성지 | 번역파일 | DB     | 사진  | 비고                                              |
| ------ | ---- | -------- | ------ | ----- | ------------------------------------------------- |
| 대전   | 27   | 27       | **27** | 8     | WYD 공식 일정지 솔뫼·해미 포함                    |
| 서울   | 25   | 25       | **25** | 12    | 사진이 가장 많다                                  |
| 대구   | 20   | 20       | **20** | 1     |                                                   |
| 수원   | 18   | 17       | 0      | 2     | 화성 순교성지 1곳은 원문이 8/17 수정되어 제외     |
| 전주   | 18   | 18       | 0      | 4     |                                                   |
| 춘천   | 16   | 16       | 0      | 2     |                                                   |
| 부산   | 11   | 8        | 0      | **0** | 기장·울산 장대·죽림굴 3곳은 원문 재작성으로 제외  |
| 의정부 | 11   | 11       | 0      | 2     |                                                   |
| 원주   | 11   | 8        | 0      | 5     | 배론·단양 김범우·남종삼 유택지 3곳 제외           |
| 안동   | 10   | 10       | 0      | **0** |                                                   |
| 인천   | 10   | 10       | 0      | 1     |                                                   |
| 마산   | 9    | 9        | 0      | 1     |                                                   |
| 제주   | 8    | 8        | 0      | 1     |                                                   |
| 청주   | 7    | **0**    | 0      | **0** | 원문이 8/17~18 재작성 — 번역 자체가 없다          |
| 광주   | 6    | 6        | 0      | **0** |                                                   |
| 군종   | 1    | 1        | 0      | **0** |                                                   |
| **계** | 208  | 194      | **72** | 39    |                                                   |

```bash
npm run translate:export -- --diocese 대구
```

**번역 용어 기준** — 앞의 52곳에서 쓴 표기를 그대로 잇는다.
St. Andrew Kim Taegon / Bishop Laurent Imbert / Fr. Pierre Maubant /
Fr. Jacques Chastan / Bishop Simeon Berneux / Fr. James Zhou Wenmo /
Byeongin·Gihae·Sinyu Persecution / beatification·canonization.
관아 이름은 음차 뒤 괄호로 역할을 밝힌다 — `Uigeumbu (State Tribunal)`.
**주소는 번역하지 않고 로마자 표기만 넣는다.** 한국어 주소가 택시 기사에게 통해야 한다.

**왜 급한가** — 아래 WYD 2027. 해외 청년 20~30만 명이 온다.

### 2. 사진 — 최대 병목 (사용자 담당)

**측정했다 (2026-08-26). 208곳 중 39곳뿐 — 169곳이 빈 껍데기다.**

| 잰 날짜    | 사진 있음 | 전체 | 커버리지  |
| ---------- | --------- | ---- | --------- |
| 2026-08-26 | **39**    | 208  | **18.8%** |

교구별 분포는 위 1번 표의 `사진` 열에 있다. **한 장도 없는 교구가 5개**다 —
부산 11곳 · 안동 10곳 · 청주 7곳 · 광주 6곳 · 군종 1곳. 서울(12/25)과
대전(8/27)에 몰려 있고 나머지는 교구당 1~5장이다.

심사 기준이 "개발 완료된 완성 서비스"이므로 이 숫자가 지금 가장 큰 위험이다.

#### ⚠️ 위키미디어 후보 목록(8/23)은 쓰면 안 된다

`data/research/wikimedia_candidates_2026-08-23.csv` 77건은 **라이선스만 확인하고
무엇을 찍은 사진인지는 확인하지 않은 목록**이다. `match: strong` 은 내용이 아니라
글자 유사도였다. 8/27 에 실제로 열어보고 확인한 것들이다.

| 성지            | 붙어 있던 후보              | 실제              |
| --------------- | --------------------------- | ----------------- |
| 우포도청 터     | `FBI police officer 2.jpg`  | 미국 FBI 경찰관   |
| 한티 순교성지   | `Khantia mansia map.png`    | 러시아 한티만시 지도 |
| 비산 성당       | `Minami Bisan-Seto Bridge`  | 일본 세토대교     |
| 성 유스티노 신학교 | `Justin McMurdie (…)`    | 저스틴이라는 사람 |
| 범어 주교좌대성당 | `Beomeo station entrance` | 지하철 범어역 출구 |

**일괄로 넣었으면 우포도청 순교터에 FBI 경찰관 사진이 올라갔다.**
이 문서가 전에 "후보 78건부터 소진하라"고 적어 두었던 그 목록이다.
문서에 적혀 있다고 믿고 실행하면 안 된다는 사례로 남긴다.

#### 8/27 재조사 결과 — 쓸 수 있는 것은 3곳뿐

169곳 전부를 한국어 이름으로 다시 검색해 154건을 받아 분류·설명까지 대조했다.
나머지는 산·시청·벽화마을 같은 것들이었다.

| 성지              | 파일                        | 라이선스     | 저자             |
| ----------------- | --------------------------- | ------------ | ---------------- |
| 계산 주교좌대성당 | `Kyesan Cathedral Front.jpg`| CC BY-SA 3.0 | Himuka tachibana |
| 범어 주교좌대성당 | `범어대성당.jpg`            | CC BY-SA 3.0 | Ewolrd2          |
| 구산 성지         | `Gusan Cathedral.jpg`       | CC BY-SA 4.0 | (확인 필요)      |

판정칸까지 넣은 전체 결과는 `data/research/wikimedia_recheck_2026-08-27.csv`.
**아직 DB 에 넣지 않았다.** 넣는 스크립트 `scripts/tmp-photo-apply.ts` 는
노트북에만 있다(`tmp-` 는 gitignore 대상).

#### 다음에 뒤질 곳 — 공공누리

한국관광공사 사진갤러리·문화재청. **아직 안 봤고, 여기서 나오면 규모가 클 수 있다.**
⚠️ TourAPI 응답 이미지는 저장 금지(ADR 0002). 공공누리와 경로를 구분할 것.

#### AI 로 성지 사진을 만들지 않는다 (2026-08-27 결정)

2차적저작물이라 저작권이 안 풀리고, 무엇보다 **AI 가 그린 배티 순교성지는
배티 순교성지가 아니다.** 실제 순교지 자리에 지어낸 그림을 올리는 것은
CLAUDE.md 의 "더미 데이터를 화면에 남기지 않는다"에 정면으로 걸린다.
**사진이 없는 편이 가짜 사진보다 낫다.**
인스타 사진은 게시자에게 DM 으로 허락을 받는 것이 유일하게 안전한 길이다.

> `data/research/image_sourcing_summary.md` 는 **195곳 기준의 옛 집계**라
> 숫자를 쓰면 안 된다. 위 표가 최신이다.

```sql
select count(*) filter (where image_url is not null) as 사진있음,
       count(*) as 전체
from holy_sites;
```

촬영 로드맵은 `docs/30-content-ops/인스타그램-서울-콘텐츠-플랜.md` 에 5회차 동선으로 있다.

조사에서 발견 — 혼자 성지 사진 프로젝트를 하는 개인 계정이 있다(24번째 성지 진행 중).
핸들은 확인 못 했다. 인스타에서 `천주교 성지 사진 프로젝트` 로 찾아 협업을 검토할 만하다.
**무단 사용 금지. 정식 연락이 먼저다.**

### 3. ~~대체 추천 기능을 화면에 붙이기~~ — 완료 (2026-08-19)

`/alternatives` 라우트에 붐빔 피하기 화면을 연결했다.
검색 → 후보 선택 → 붐빔 + 대체 성지 표시의 2단계 UX 이고,
홈 화면 "오늘의 쉼표" 아래에 진입 카드("가려던 곳이 붐비나요?")가 있다.
스타일은 홈 화면과 같은 앱 토큰(`app-*`·`brand-violet`)을 따랐다 —
디자인을 바꾸고 싶으면 `AlternativesPage.tsx` 와 `AlternativesList.tsx` 만 고치면 된다.

**아직 실제 TourAPI 로 눌러보진 못했다** (웹 세션은 TourAPI 차단).
로컬에서 `npm run dev` 로 열어 검색부터 추천까지 한 번 눌러볼 것.

### 3.5. ~~스탬프에 한 줄 기록~~ — 코드 완료 (2026-08-19), DB 적용 필요

컨셉 축 3("다녀온 사람이 다음 사람에게")을 구현했다. 스탬프를 찍으면
한 줄을 남길 수 있고, 성지 상세에 "다녀온 사람의 한 줄"로 익명 공개된다.

**로컬에서 이것부터**: `npm run db:migrate` — 새 마이그레이션
`20260819000000_add_stamp_note.sql` 이 note 컬럼·update 정책·공개 뷰
(`site_visit_notes`, user_id 제외)를 만든다. 적용 전에는 한 줄 저장이
note 컬럼이 없어 실패한다 (스탬프 찍기 자체는 동작).

### 3.6. ~~여행기 작성 화면~~ — 완료 (2026-08-19)

로드맵 2단계 항목. `/records` 의 죽어 있던 "여행기 작성하기" 버튼에
인라인 작성 폼(`LogComposer`)을 붙였다 — 성지 선택(스탬프 찍은 곳 우선),
방문일, 제목, 내용. DB 는 기존 `pilgrimage_logs` 그대로라 마이그레이션이 필요 없다.

### 3.7. ~~즐겨찾기~~ — 코드 완료 (2026-08-19), DB 적용 필요

로드맵 3단계 항목. 성지 상세의 하트 버튼이 실제로 저장되고(핑크로 채워짐),
탐색 화면 맨 위에 "즐겨찾는 성지" 목록이 뜬다. 비로그인 시 로그인으로 보낸다.

**로컬에서**: `npm run db:migrate` — `20260819010000_create_favorites.sql`
(favorites 테이블 + RLS). 스탬프 한 줄(3.5)과 같이 한 번에 돌리면 된다.

### 코드 작업 정리 — 웹 세션에서 가능한 기획 항목은 소진

이번 정비(8/19)로 기획 문서가 약속한 것 중 코드로 만들 수 있는 항목은
다 만들어졌다. 이제 남은 것은 전부 로컬·현장·사람 손이 필요한 일이다:

- AI 가이드 Edge Function 실배포 (`supabase functions deploy`, 로컬)
- 지도 실연동 (ADR 0003 — 카카오 키 도메인 등록, 로컬 확인)
- 전례력 시기 판정 — 사목자 확인 (사람)
- 번역 감수 `machine` → `human` (사람)
- 촬영 194곳 (현장)
- 마이그레이션 2개 적용 — `npm run db:migrate` (로컬 1분)

### 3.8. 도슨트 기능 — 조사 체계 완성 (2026-08-20), 현장조사 대기

성지 입구부터 성당 내부(건축·부조·스테인드글라스·성상)까지 핸드폰이
설명해주는 도슨트가 이 앱의 차별점이다. 비신앙인에게도 가치가 있다.

- 조사 절차: `docs/30-content-ops/도슨트-현장조사-매뉴얼.md`
- 데이터 형식: `data/docent/_템플릿.json`
- **어느 세션이든 "○○성지 도슨트 조사 시작"이라고 하면 Claude 가
  절차를 안다** (`.claude/skills/docent-survey` 저장소 스킬)

다음 순서: 사용자가 첫 성지(서울 추천: 절두산·명동·약현)에서 파일럿
조사 1곳 → 형식 검증 → 3~5곳 쌓이면 화면 구현(포인트 카드 + 기존 TTS).
화면을 먼저 만들지 않는다 — 더미 금지 원칙.

### 3.9. ~~주변 본당·피정의집 안내~~ — 완료 (2026-08-24)

로드맵 3단계 "catholic_directory 활용". 성지 상세에 반경 5km 본당·공소·
피정의집을 거리순으로 보여준다(전화 걸기 포함). 자체 데이터라 캐싱 자유.
미사 시간은 적지 않고 "전화로 확인"을 안내 — 틀린 시간표보다 낫다.
DB 변경 없음(기존 테이블·공개 읽기 정책 사용).

### 3.10. ~~마음 나침반 응답 저장~~ — 코드 완료 (2026-08-24), DB 적용 필요

로드맵 3단계. 7문항 답과 추천 결과를 저장하고(로그인 시에만, 비로그인은
조용히 건너뜀), 다시 열면 인트로에 "지난번엔 이곳을 권해드렸어요"가 뜬다.
추천 개인화의 원료가 쌓이기 시작한다.

**로컬에서**: `npm run db:migrate` — `20260824000000_create_compass_responses.sql`

### 3.11. ~~영어 번역을 화면에 연결~~ — 완료 (2026-08-24)

치명적 공백을 메움: 앱이 `holy_site_translations` 를 전혀 읽지 않아
**번역 194곳을 import 해도 화면에 안 나오는 상태였다.** 이제 언어를
영어로 바꾸면 성지 상세의 이름·소개·역사가 번역본으로 나오고, 주소
아래 로마자 표기가 병기되며, TTS 도 영어 본문을 읽는다. 번역이 빈 칸은
한국어로 폴백 — 빈 영어 화면보다 한국어가 낫다.

**전제**: 번역 13개 파일의 DB import (로컬 `translate:import`). 그 전에는
영어 모드에서도 한국어가 나온다 (정상 폴백).

### 4. WYD 2027 을 콘텐츠·발표에 반영

조사에서 확인된 **확정 사실**이다. 상세는 `docs/30-content-ops/SNS-레퍼런스-조사.md` 3장.

- 교구대회 **2027.7.29~8.2**, 서울 제외 **15개 교구**
- 본대회 2027.8.3~8.8, 서울
- 최대 80만 명, **해외 청년 20~30만 명**
- 수호성인에 **성 김대건 안드레아와 동료 순교자**
- 공식 일정에 **솔뫼성지**, **해미읍성** — 둘 다 우리 DB 에 있다

할 일:

- [ ] 인스타 편성표에 D-day 코너·수호성인 시리즈 추가
- [ ] 한·영 병기 캡션을 기본값으로
- [ ] 공유 유도 CTA 를 캡션 규격에 넣기 (현재 없음)
- [ ] 순례 여권(스탬프) 연계 콘텐츠
- [ ] **공모전 발표자료에 WYD 를 근거로 넣기** — 앱이 필요한 이유가 날짜·규모까지 확정돼 있다

### 5. `holy-pilgrimage-backend` 저장소 삭제 (사용자 담당)

백업은 `보관_구버전저장소/` 에 있다 — **저장소 안이 아니라 노트북 쪽 폴더다.**
삭제 전에 그 폴더가 실제로 있는지 노트북에서 먼저 확인한다. (이 저장소에서는 확인 불가.)
**Claude 는 못 한다** — 토큰이 `linkcontent7-huisun` 이고 소유자는 `nohhuisun` 이라 권한이 없다.
GitHub 웹에서 직접 삭제해야 한다.

---

### 6. 저장소 안에 남아 있는 남의 폴더 두 개 (사용자 판단)

2026-08-26 로컬에서 **실제로 있는 것을 확인했다** (웹 세션은 추정만 했었다).

| 폴더                              | 내용                                      | 파일 수  |
| --------------------------------- | ----------------------------------------- | -------- |
| `supabase/visitholykorea_버전보관/` | v1·v2·v3 구버전 앱 사본. `node_modules` 와 중첩 `.git` 포함 | **8,778** |
| `visitholykorea-학습자료/`          | 학습 PDF·이미지·아이디어 모음집           | 242      |

`.gitignore` 와 `eslint.config.js` 에 제외 처리해서 **린트 오류 6,405건은 사라졌다.**
다만 디스크에는 그대로다. ADR 0005 가 "구버전 사본에서 코드를 꺼내 쓰지 말라"고
못박은 그 사본이고, 특히 앞의 것은 `supabase/` 아래 있을 이유가 전혀 없다.
**저장소 밖(OneDrive 등)으로 옮기길 권한다.** 지우는 건 사람이 판단할 일이라 두었다.

### 7. `AlternativesPage` 로컬 초안의 좋은 부분 옮겨 붙이기 (급하지 않음)

PR #8 에서 로컬 초안을 채택하지 않았다 — 원격 판이 TourAPI 호출을 300ms
디바운스하고 넓은 화면 설정을 지키기 때문이다. 다만 초안에만 있던 두 가지는
가져올 만하다: **첫 화면의 3단계 사용법 안내**, **검색어 지우기 X 버튼**.

```bash
git show befa7e7:src/pages/AlternativesPage.tsx
```

## 작업 환경 — 어디서 무엇을 하는가

세션이 두 종류라 헷갈리기 쉽다. **할 수 있는 일이 다르다.**

|                        | 노트북 파일 | 외부 인터넷   | 쓰는 곳                             |
| ---------------------- | ----------- | ------------- | ----------------------------------- |
| 로컬 CLI · 데스크톱 앱 | 볼 수 있음  | 전부 됨       | DB 작업, OneDrive 정리, 배포 확인   |
| 웹 (claude.ai/code)    | **못 봄**   | 허용 도메인만 | 저장소 안에서 끝나는 코드·문서 작업 |

웹 세션은 GitHub 저장소만 복제해온 클라우드 컨테이너에서 돈다.
OneDrive 든 `.env.local` 이든 노트북 안의 것은 구조적으로 접근이 안 된다.
인터넷은 허용 목록에 있는 도메인만 나간다 — 2026-08-19 실측으로
supabase.com·vercel.app·TourAPI(data.go.kr) 는 차단, 문서 사이트류는 일부 열림.
환경 설정(claude.ai/code 의 환경 선택기)에서 허용 도메인을 늘릴 수는 있지만,
비밀값 저장소가 없어 DB 비밀번호를 웹 환경에 넣는 것은 권장되지 않는다.

---

## 확인 못 한 것 — 나중에 채울 것

- ~~번역의 DB import 여부~~ — 2026-08-26 측정 완료: 72/194 (위 1번)
- ~~사진 실제 커버리지~~ — 2026-08-26 측정 완료: 39/208 (위 2번)
- 서비스 롤 키가 왜 `JWT issued at future` 인지의 근본 원인.
  노트북 시계·anon 키는 정상임을 확인했으니 키 자체를 새로 받으면 되지만,
  Supabase 쪽에서 JWT 시크릿이 회전됐는지는 대시보드에서 확인해야 안다
- 인스타 계정들의 실제 참여 수치(좋아요·공유·저장). 스크래핑이 막혀 있어 눈으로 세야 한다
- 성지 사진 프로젝트 계정 핸들
- 본당 단위 계정 전수 조사 (표본만 봤다)

---

## 낡은 문서 — 숫자를 그대로 믿지 말 것

아래 문서들은 기록으로서는 가치가 있지만 **현재 상태의 근거로 쓰면 안 된다.**
숫자가 필요하면 이 문서나 DB 를 본다.

| 문서                                      | 어긋난 곳                                            |
| ----------------------------------------- | ---------------------------------------------------- |
| `data/research/image_sourcing_summary.md` | 195곳 기준. 지금은 208곳이고 이후 19장이 더 들어갔다 |
| `docs/00-overview/2026-08-13-*.md`        | 8/13 시점 스냅샷. 이후 커밋 8개가 반영 안 됨         |
| `CLAUDE.md` 의 "195곳"                    | 초기 수집분 기준임이 명시돼 있다. 실측은 208곳       |
