# 최성빈 님께 보낼 답장 문안 (2026-09-07)

받은 메일: 「Visit Holy Korea 반응형 패치 전달 — 적용 순서 안내」 (9/7 02:06)
회신 상태: **아직 안 보냄 — 사장님 확인 후 발송**

> 연락처는 여기 적지 않는다. 저장소가 공개라 개인 정보는 남기지 않는다.

---

## 제목

```
Re: Visit Holy Korea 반응형 패치 전달 — main 병합 완료 + 이후 부탁
```

## 본문

```
성빈 님, 안녕하세요.

보내주신 반응형 패치 잘 받았습니다. main에 병합해서 배포까지 마쳤습니다.
먼저 감사드립니다. 덕분에 앱이 웹앱다워졌습니다.


## 1. zip 대신 PR #13으로 받았습니다

메일에 압축 풀고 파일 9개를 손으로 복사하는 순서를 아주 자세히
적어주셨는데, 같은 내용이 이미 PR로 올라와 있어서 그쪽으로 받았습니다.
손 복사보다 실수가 적고 되돌리기도 쉬워서요. 안내는 그대로 따랐습니다 —
main에 바로 넣지 않고 별도 브랜치에서 확인한 뒤 합쳤습니다.

**PR #13은 내용이 전부 들어갔으니 닫아주셔도 됩니다.**
제 쪽에서 임의로 닫지 않고 남겨두었습니다.


## 2. 그대로 가져간 것

- TopNav / nav-items / PageContainer 신규
- AppLayout의 폭 전환 (1024px 기준 상단 내비 ↔ 하단 탭)
- MapPage 좌우 분할 (목록 452px + 지도)
- HomePage / ExplorePage 격자
- TodayQuietSection의 padded 프롭

특히 **본문을 512px로 묶던 "가짜 휴대폰 액자"가 사라진 것**이 컸습니다.
데스크톱에서 히어로 사진이 1265×420, 성지 격자가 270×270으로 커졌습니다.
사진이 주인공인 앱이라 이 차이가 그대로 인상 차이가 되더군요.

KEEP_BOTTOM_NAV_ON_MOBILE = true로 두신 판단에도 동의합니다.
주 사용자가 50대 이상 85%라, 익숙한 조작 위치를 한 번에 빼는 건
위험하다고 봤습니다. 기본값 그대로 두었습니다.


## 3. 한 곳만 조정했습니다 — 홈의 AI 가이드

보내주신 판은 「칩 3개 + 큰 AI 배너」를 데스크톱에서 나란히 놓는데,
그 큰 배너는 며칠 전에 이미 걷어낸 것이었습니다. 화면 4분의 1을 먹어서
그 아래 「쉼표 순례길」이 첫 화면에서 한참 밀려 있었거든요.

그래서 보내주신 반응형 격자는 그대로 살리고, 넷을 한 격자에 넣었습니다.

    grid-cols-2 lg:grid-cols-4

휴대폰에서는 2×2, 데스크톱에서는 한 줄로 펴집니다. AI 가이드는 보라색
바탕을 남겨서 여전히 먼저 눈에 들어옵니다. 요소를 두 벌 그리지 않으니
상태도 하나뿐이고요.

데스크톱에서 나란히 두신 의도 자체는 좋았습니다. 다만 모바일에서
배너가 다시 붙는 게 걸려서 이렇게 절충했습니다.


## 4. 작은 버그 하나 고쳤습니다

1024px에서 상단 메뉴 6개가 gap-6으로 벌어지면서 글자가 두 줄로
깨졌습니다. 「순례 코 스」 「마음 나침 반」처럼요.
whitespace-nowrap을 주고 1024~1280 구간만 gap을 좁혔습니다
(xl 이상은 원래대로 gap-6).


## 5. 검증 결과

npm run verify 통과, 테스트 334개.

폭별로는 iframe에 앱을 띄워서 확인했습니다. Chrome 창이 658px 아래로
안 줄어들어서 375px를 직접 못 보겠더군요. iframe 안에서는 미디어 쿼리가
그 폭으로 평가돼서 진짜 값이 나옵니다.

  뷰포트    상단 내비   하단 탭   칩     가로 스크롤
  371px     없음        있음      2열    없음
  768px     없음        있음      2열    없음
  1024px    있음        없음      4열    없음
  1280px    있음        없음      4열    없음

칩 하나가 148×87이라 탭 영역 44px 기준도 넘습니다.

시안 참고 파일(doc-page.js, support.js)이 lint 오류를 133건 냈는데,
앱 빌드에 안 들어가는 문서라 eslint 무시 목록에 넣었습니다.
"넣지 말 것"이라고 적어주신 그대로입니다.


## 6. 아직 못 본 것

- 큰 글자 모드(118%)를 폭별로 확인하지 못했습니다
- 실기기(안드로이드·아이폰) 확인도 아직입니다

혹시 성빈 님 쪽에서 큰 글자 모드로 넓은 화면을 한 번 봐주실 수 있으면
도움이 되겠습니다. 주 사용자층 때문에 이 조합이 특히 중요합니다.


## 7. 앞으로 부탁드리고 싶은 것

일정을 먼저 말씀드리면, 관광공모전 기능 동결이 9월 14일,
제출이 9월 21일입니다. 시간이 많지 않습니다.

이런 것들을 함께 봐주시면 정말 큰 힘이 되겠습니다.

**(1) 기능 하나하나 눌러보면서 버그 찾기**

성지 상세의 오디오 도슨트, 순례 스탬프, 마음 나침반 8문항, 붐빔 피하기,
축제 가는 김에 — 만든 사람은 되는 길로만 눌러봅니다. 처음 보는 눈으로
이상한 걸 발견해주시면 그게 제일 값집니다.
언어를 영어·스페인어로 바꿔서 보시면 더 잘 보일 겁니다.

**(2) 랜딩(첫 화면)을 더 직관적으로**

지금 첫 화면이 처음 온 사람에게 "이 앱이 뭘 해주는지" 3초 안에
전달되는지 자신이 없습니다. 무엇을 먼저 보여주고 무엇을 뒤로 미뤄야
할지, 순서와 덜어낼 것에 대한 의견을 듣고 싶습니다.

**(3) 다른 앱에서 가져올 만한 것**

여행·지도·오디오 가이드 쪽 앱들을 보시다가 "이건 우리도 쓰면 좋겠다"
싶은 화면이 있으면 캡처와 함께 알려주세요. 국내외 아무거나 좋습니다.
성지순례라는 성격상 요란한 것보다 조용하고 읽기 편한 쪽이 맞다고
보는데, 그 결을 지키면서 더 나은 방법이 있을 것 같습니다.

**(4) 반응형 나머지**

PATCH-README에 남겨주신 다음 단계 네 가지도 그대로 살아 있습니다.
router.tsx 정리, SiteDetailPage 데스크톱 2열, 나머지 화면 PageContainer,
지도 마커 클러스터링. 여유가 되시면 이어서 봐주셔도 좋겠습니다.


급하게 부탁드려 송구합니다. 보내주신 작업이 눈에 보이게 앱을 바꿔놨고,
무엇보다 "왜 이렇게 했는지"를 문서로 남겨주신 게 큰 도움이 됐습니다.
남은 2주, 함께 잘 마무리했으면 좋겠습니다.

감사합니다.

노희선 드림
```

---

## 보내기 전 확인할 것

- [ ] 사장님이 문안을 읽고 어색한 표현을 고쳤는가
- [ ] PR #13 을 닫아달라는 요청이 들어 있는가 (있음, 1절)
- [ ] 마감 일정(9/14 · 9/21)을 알렸는가 (있음, 7절)
- [ ] 부탁 네 가지가 구체적인가 — "잘 봐달라"가 아니라 무엇을 어떻게
