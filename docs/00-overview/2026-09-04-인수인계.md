# 인수인계 — 2026-09-04

**새 창에서 이어서 하려면 이 문서 하나만 읽으면 된다.**
지금 당장 할 일은 `docs/이어서-할-일.md` 맨 위, 이 문서는 **어디까지 왔는지**를 담는다.

---

## 0. 시작하는 법

```bash
git checkout main && git pull
npm ci            # 웹 세션이면 node_modules 가 없다
npm run verify    # 타입·린트·테스트 306개
```

| 세션 | 할 수 있는 것 |
| --- | --- |
| **로컬 CLI**(오렌지 창, `~\visitholykorea app`) | DB·마이그레이션·TourAPI 실호출·실기기·배포 확인 |
| **웹 세션**(claude.ai/code) | 저장소 안 코드·문서 작업, GitHub PR. **앱을 띄울 수 없다** |

---

## 1. 지금 상태 (2026-09-04 실측)

| 항목 | 값 |
| --- | --- |
| 테스트 | **306개 통과** (39개 파일) |
| 사진 | **53곳 / 208 (25.5%)** — 최대 약점 |
| 실사용자 | **가입자 1명 · 스탬프 0** — 두 번째 약점 |
| 열린 PR | **없음** (#12 까지 전부 머지) |
| 미커밋 | **없음** — main 과 origin/main 이 같다 |
| 배포 | https://visitholykorea-app.vercel.app · 최근 배포 success |

### 마감

| 마감 | 무엇 | 상태 |
| --- | --- | --- |
| **9/17(목) 16:00** | '모두의 창업' 2차 온라인 신청 | 원고 완성 → `docs/10-product/모두의창업-2차-도전신청서.md` · 이미지 10장 캡처만 남음 |
| **9/21(일) 16:00** | 관광공모전 개발 완료 (**기능 동결 9/14**) | 코드 완료 · 발표자료 초안 완성 |

---

## 2. 이번 세션(9/4)에 한 일

PR #12 를 머지한 뒤, **머지 결과를 아무도 확인하지 않은 상태**여서 검수부터 했다.

**검수 결과 (배포판, 서비스워커 캐시 지우고)**

- 라우트 13개: JS 오류 0 · 렌더링 정상
- 코스 연재물: 화 번호·진행도·**다음 화 예고** 전부 정상
- 빈 여권 미리보기: 정상 (버그 1건 발견 → 아래)
- 무장애 섹션: 승인 전이라 **오류 없이 조용히 접힘** — 의도대로
- 모바일 375px: 가로 넘침 없음

**고친 것 3가지**

1. 빈 여권의 "서울에서 약 0km" → "서울 안에 있어요". 출발지가 시·도 대표
   좌표라 같은 시 안이면 0에 수렴한다. 0km 는 고장으로 읽힌다
2. `usage-check` 가 검수용 테스트 계정을 실사용자로 셌다. 이 숫자가
   사업계획서에 들어가므로 없는 사용자를 있다고 말할 뻔했다 → 분리 표기
3. `verify` 중 테스트 1개가 시간초과로 간헐 실패 → waitFor 1초 → 5초

**사진 51 → 53곳.** 수집 방향을 뒤집어서 얻었다. 지금까지는 전부 검색어
기반이라 검색어에 안 걸리는 이름은 후보에도 못 들었다. 이번엔 국가유산청
시도별 목록을 **전부 받아 좌표로 먼저 좁혔다**(약 14,000건).
강경 성당·언양 성당 확보(둘 다 공공누리 제1유형, 눈으로 확인).

**추가로 한 일 (9/4 후반)**

도슨트 플레이어 문구를 **6개 국어**로 만들었다. 앱 전 화면은 6개 국어인데
정작 핵심 기능인 도슨트만 한/영뿐이라, 스페인 순례자가 재생 버튼 앞에서
영어를 만나는 상태였다. 원인은 문구가 컴포넌트 안 상수에 박혀 있어서
타입 검사가 누락을 못 잡은 것 — 공용 사전으로 옮겨 다시는 빠질 수 없게 했다.
**배포판에서 6개 국어 전부 확인 완료**(ko/en/es/fr/pt/it, 한국어 잔존 0).

⚠️ 다만 **도슨트 원고 본문은 여전히 한·영뿐**이다(13곳). 화면 문구는 6개
국어인데 낭독 내용은 두 언어다. 원고 번역은 분량이 커서 9/14 동결 전에
전부는 어렵다 — **대표 3~4곳만 스페인어로** 늘리는 것이 현실적이다.

---

## 3. 🔴 남은 일 — 사람이 해야 하는 것

**우선순위 순. 1번이 나머지 전부의 병목이다.**

1. **베타 테스터 9명 연락** — `docs/30-content-ops/베타테스터-안내문.md` 의
   문구를 **2~3명씩** 나눠 보낸다. 첫 3명 반응을 보고 고친 뒤 다음 사람에게.
   스탬프 0이면 여권·별자리·코스 진행도·사진 승격이 전부 빈 화면이고,
   10월 심사에서 보여줄 것이 없다
2. **data.go.kr 무장애 여행정보 활용신청** — 9/2 실측 403 "등록되지 않은
   서비스키". 404 가 아니므로 **경로는 맞고 신청만 남았다.** 승인되면
   코드 수정 없이 성지 상세에 섹션이 저절로 나타난다
3. **9/17 모두의창업 신청** — 원고는 준비됨. 이미지 10장 캡처만
4. **성지 사진 촬영** — 자동 수집은 사실상 소진됐다(제1유형이 드물다).
   권역별로 하루 3~5곳, 서울·대전처럼 몰린 곳부터
5. **실기기 확인**(안드로이드·아이폰) — 9/14 이전. 9/1 흰 화면 사고가
   실제로 고쳐졌는지. **어떤 폰이었는지 아직 모른다**

---

## 4. 다음 세션이 코드로 할 수 있는 것

사람 일이 막혀 있어도 이건 진행할 수 있다.

- **사진 전수훑기 확장** — 이번 방식(좌표 우선)이 남은 경로 중 가장 넓다.
  다만 공공누리 제1유형이 드물어 수확은 적다
- **도슨트 원고 확대** — 현재 13곳. 사진 0%인 교구부터
- ~~다국어 플레이어 문구~~ — ✅ 9/4 완료(6개 국어, 배포 검증까지)
- **도슨트 원고 본문 다국어** — 대표 3~4곳만 스페인어로 (분량상 전부는 무리)
- 발표 시연 대비: 로그인 없이 보이는 화면부터 점검

---

## 5. 지켜온 원칙 (깨지 말 것)

1. **TourAPI 응답을 저장하지 않는다** — 실격 위험 (ADR 0002)
2. **더미 데이터를 화면에 남기지 않는다** — 검수용 데이터는 쓰고 지운다
3. **확인 못 한 것은 "확인 못 했다"고 적는다** — 추측을 사실처럼 쓰지 않는다
4. **숫자를 인용할 땐 언제 잰 값인지 함께 적는다**
5. **작업이 끝나면 그 자리에서 `docs/이어서-할-일.md` 를 고친다**
6. **창을 옮기기 전에 푸시하고, 열자마자 `git pull`** — 세션끼리는 서로를 모른다
