# 인수인계 — 2026-09-12

**새 창에서 이어서 하려면 이 문서 하나만 읽으면 된다.** 지금 당장 할 일은 `docs/이어서-할-일.md` 맨 위,
이 문서는 오늘 무엇을 어떻게 확인했는지를 숫자로 남긴다. 사장님 인터넷이 끊기기 직전(18:00) 작성.

## 오늘의 결과 한 줄

**v6.0(첫 공개 배포판) 태그 → 제출판 정리 착수.** 홈을 본질(조용한 성지 일정) 순서로 다시 짜고,
글자 크기 3단계·영문 주소·하단 탭 고정·가까운 성지 전체 목록을 넣었다. 마지막 커밋 `7f5905d`.

## 커밋 (시간순, 전부 origin/main)

| 커밋 | 내용 | 확인 방법 |
| --- | --- | --- |
| `dad36b0` | AI 가이드 모델 `gemini-3.6-flash` 로 교체·배포 | curl → HTTP 200, 절두산 질문에 정확한 답 (23:35) |
| `b5be386` | 임시 사진 6장 이름 해시 배정, 관리자 편집 칸 확장(이름·요약·팩스·주변 안내) | 400px 헤드리스 목록·상세 |
| `b146f8e` | 외국어 화면 영문 주소 (목록 13곳·상세·찾아가는 길) + 영어 잔여 3건 | 영어 모드 헤드리스 |
| `4848beb` | 하단 탭 고정 — body 대신 `ScrollShell` 상자 스크롤 | 상자 1500px 스크롤, body 0 |
| `14fbeb1` | **v6.0** 태그·GitHub Release·CHANGELOG (v1.0.0 → v6.0 으로 바꿈) | Releases 페이지 |
| `62e2655` | 글자 크기 소·중·대 — 상단바 「T」→ 옆에 펼침. 100/112/125% | 닫힘·펼침·대 3상태 400px |
| `aa8d618` | 홈 「여기에서 가장 가까운 성지·성당」 입구 + `/nearby` 전체 목록(207곳·10km 본당) + 탭 「설정」 | 거부 대체 상태 확인 |
| `425c468` | 문서 재정립 — 폴더마다 README(확인하고 싶은 것 → 파일), 71개 이동, 참조 41곳 교정 | 깨진 링크 4 = 로컬 전용 문서 |
| `2a5f6e5` | 홈 재구성(모바일): 히어로→가까운 성지→마음 나침반→고요 속으로→격자. 탭 5→4, AI 는 상세로 | 400px 헤드리스 |
| `a17b2b7` | PWA 아이콘 VISIT HOLY 워드마크 4종, 설정 「홈 화면에 추가」, ≡ 버튼 당김 | — (실기기 필요) |
| `dd6dd5b` | 나침반 「이 코스로 가볼게요」 되돌아가던 버그 + `#directions` 로 찾아가는 길 펼침 + 5장 3+2 정렬 · **Codex T-012 rem 치환 130곳 포함** | 8문항 헤드리스 통과 → 상세 도착·방문정보 펼침 |
| `7f5905d` | PC 정리 — 히어로 버튼이 입구, 중복 카드 lg 숨김, 상단 「탐색」 제거, 검색 문구, 히어로 6개 국어 | 1280px 헤드리스 |

숫자: 테스트 349 통과(매 커밋) · 성지 208 · 사진 53 · 도슨트 13 · 화면 문구 키 ≈ 450.

## 사장님이 정한 것

- 마음 나침반 문항 그대로(축약 안 함) · 디자인 변경 없음(배치·빼고 넣기만) · `visitholy작업설계도_260904.txt` GitHub 유지
- 버전은 v3~v5 흐름을 이어 **v6.0**
- Codex 는 시키라고 함 → T-012 Orca 로 실행·완료. T-013 은 미착수

## 값·키 상태 (내용은 적지 않는다)

- `.env.local`: `SUPABASE_ACCESS_TOKEN`(30일 만료, 10/12경) · `NAVER_MAP_CLIENT_ID/_SECRET` · `VITE_NAVER_MAP_CLIENT_ID` 있음
- Supabase Secrets: `GEMINI_API_KEY` 등록됨 (무료 등급)
- 네이버 지도: **인증 실패** — 콘솔에서 Web Dynamic Map 체크 + URL(`http://localhost:5173`, `https://visitholykorea-app.vercel.app`) 등록 필요

## 다음 담당자가 할 일 (순서대로)

1. `git pull` → `npm run verify` (349 기대)
2. **Codex T-013** (제출판 숨김 플래그) — `docs/70-agent-workspace/tasks/T-013-제출판-숨김플래그/지시서.md`.
   Orca: `orca orchestration worker-start --run run_dcc087c00a96 --spec "<지시서 읽고 수행>" --agent codex --worktree current`
   → 프롬프트가 입력창에 걸려 있으면 `orca terminal send --terminal <handle> --enter`
3. 네이버 키 인증 확인(`scratchpad` 의 nv 테스트 방식: localhost:5173 에서 maps.js 로드) → 상세·지도 탭에 지도
4. 실기기 확인 목록: 하단 탭 고정 · 현재 위치 허용 경로 · 관리자 사진 업로드 · 글자 「대」 · 홈 화면에 추가 아이콘
5. 9/14 동결 → 9/21 제출: 캡처(홈·상세·도슨트·영어·반응형)·설명서

## 함정

- Orca 로 띄운 Codex 는 프롬프트가 입력창에 붙기만 하고 전송이 안 된다 — Enter 를 보내야 한다 (오늘 재현)
- Orca 의 Codex 는 승인 없이 돈다(YOLO). git 쓰기 금지·`.env.local` 금지는 지시서 문장으로만 막고 있다
- 헤드리스 크롬은 GPS 를 항상 거부한다 — 현재 위치 성공 경로는 실기기로만
- `text-[..px]` 는 예외 6개 파일(상단·하단 바·관리자·피커)에만 남아 있어야 한다. 새 코드에 px 글자를 넣지 말 것
