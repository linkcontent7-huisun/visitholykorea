# 인수인계 — 2026-08-24

8/19 인수인계 이후 닷새간의 변화. **현재 할 일은 `docs/이어서-할-일.md`**,
이 문서는 경위와 판단 근거를 남긴다.

## 이 기간에 끝난 것

### 저장소가 하나로 합쳐졌다

세 화면(웹 세션·로컬 CLI·배포 사이트)이 서로 다른 버전을 보던 문제를
해결했다. 웹 세션의 푸시 권한이 열리면서(8/22) 작업 브랜치를 PR 로 main 에
머지하는 흐름이 생겼고, **PR #1~#6 이 전부 main 에 들어갔다.** 이제 규칙은
하나다 — 작업 단위가 끝나면 바로 main 머지. 브랜치를 쌓아두지 않는다.

### 기능 7개 (전부 main·배포 반영)

| 기능                                     | PR  |
| ---------------------------------------- | --- |
| 붐빔 피하기 (`/alternatives`)            | #1  |
| 스탬프 한 줄 기록 (익명 공개)            | #1  |
| 여행기 작성 폼                           | #1  |
| 즐겨찾기                                 | #1  |
| 주변 본당·피정의집 (주소록 5,918건 활용) | #4  |
| 마음 나침반 기억 ("지난번엔 이곳을")     | #5  |
| **영어 번역 화면 연결**                  | #6  |

#6 이 특히 중요했다 — 앱이 `holy_site_translations` 를 전혀 읽지 않아,
번역을 아무리 쌓아도 화면에 나올 길이 없었다. 기획 재검증이 잡아낸 구멍.

### 영어 번역 194/208곳

웹 세션에서 스냅샷(8/10) 원문으로 13개 교구 파일 완성(`data/translations/`).
남은 14곳(청주 7·원주 3·부산 3·수원 화성)은 8/17~18 원문 재작성분이라
**로컬 export 로만** 가능. 이미 번역된 대전 25곳·절두산도 이후 국문이
바뀌어 감수 때 대조 필요.

### 협력자가 생겼다

**seongbin45** (코딧세이 동료)가 포크로 공모전 기능설명서 + 공식 pptx 를
기여(PR #2). 저장소 권한은 없고(collaborator 는 소유자뿐) 제안만 가능한
구조. 이틀 사이 낡은 사실은 후속 커밋으로 정정(#3).

### 문서·창업 준비

순례지원팀 제안서(아티팩트+Word) · 린 캔버스 · 사업계획서 초안(PSST) ·
창업 마감일 추적표 · 도슨트 현장조사 체계(+`docent-survey` 저장소 스킬).

### 코드 리뷰가 잡은 것 (매번 돌릴 가치가 있다)

- 8/19 문서 리뷰: 결함 8건 (커밋 수 오기, 낡은 인용 등)
- 8/24 기능 리뷰: 결함 4건 — **나침반 기억 캐시가 사용자 간 새는 문제**
  (정적 쿼리 키), TTS 가 영어를 한국어 음성으로 읽는 문제, 로마자 주소
  줄바꿈, gender 저장 누락. 전부 수정됨.

## 지금 걸려 있는 자동 알림

| 시점      | 내용                                                    |
| --------- | ------------------------------------------------------- |
| 8/30 아침 | 코딧세이 2ROUND 최종 제출 확인 (**마감 8/31 월 14:00**) |
| 9/1       | 모두의 창업 2기 공고 확인 (8월말~9월초 개시 예정)       |

## 다음 세션이 알아야 할 것

1. **로컬 숙제가 밀려 있다** — `npm run db:migrate` (마이그레이션 3개:
   한줄기록·즐겨찾기·나침반) + 번역 13개 파일 `translate:import`.
   안 하면 배포판의 새 기능이 저장 오류를 내고, 영어 모드는 폴백만 나온다.
2. **코딧세이 2라운드** — 선제출 후 수정 가능. 구글폼 질문 항목을 받으면
   사업계획서 초안을 항목별로 재단한다.
3. 웹에서 가능한 로드맵은 소진 — 남은 것은 지도 실연동(로컬)·AI 하루
   코스(TourAPI 실호출)·도슨트(현장)·촬영(현장)·전례력(사목자).

## 교훈

- **연결 안 된 데이터는 없는 데이터다.** 번역 194곳이 화면에 나올 길이
  없었다. 파이프라인을 만들면 끝까지 — 화면에 나오는 것까지 확인한다.
- **리뷰는 사치가 아니다.** 두 번의 리뷰가 12건을 잡았고, 그중 하나는
  개인 기록 누수였다.
- **자주 머지가 답이다.** 22커밋을 쌓아둔 것이 세 화면 분열의 원인이었다.
