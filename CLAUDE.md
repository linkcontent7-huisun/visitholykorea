# CLAUDE.md

이 저장소에서 작업할 때 참고할 규칙이다. 사람에게도 그대로 적용된다.

## 작업을 시작하기 전에

**`docs/이어서-할-일.md` 부터 읽는다.** 지금 무엇이 끝났고 무엇이 남았는지가 거기 있다.
세션은 이전 대화를 기억하지 못하므로, 상태를 아는 유일한 방법이 이 파일이다.

그리고 **작업이 끝나면 그 파일을 그 자리에서 고친다.** 나중에 몰아서 하지 않는다.
2026-08-19 에 문서가 열흘 넘게 방치돼 "이미 끝난 일을 다시 하려던" 사고가 있었다.
경위는 `docs/00-overview/2026-08-19-인수인계.md`.

숫자(성지 수·사진 수·테스트 수)를 문서에서 인용할 때는 **언제 잰 값인지** 같이 적는다.
확인하지 못한 것은 추측해서 적지 말고 "확인 못 했다" 라고 적는다.

### 웹 세션과 로컬 세션은 할 수 있는 일이 다르다

claude.ai/code 로 여는 **웹 세션은 저장소만 복제해온 클라우드 컨테이너**에서 돈다.
노트북 파일(OneDrive·`.env.local`)은 어떤 설정으로도 못 보고, 따라서 **DB 도 못 본다.**
인터넷은 허용 목록 도메인만 나간다 (supabase·vercel·TourAPI 는 차단 — 2026-08-19 실측).

- DB 작업 · OneDrive 자료 정리 · 배포 확인 · 실제 데이터 확인 → **로컬 세션(CLI·데스크톱 앱)**
- 저장소 안에서 끝나는 코드·문서 작업과 `npm run verify` → 웹 세션도 가능

웹 세션에서 검증할 때는 `npm ci` 부터 한다. 컨테이너가 새로 뜨면 `node_modules` 가 없다.

## 이 프로젝트가 무엇인가

한국 가톨릭 성지 208곳을 다루는 순례 웹앱. 웹 · Android · iOS 를 한 코드베이스로 지원한다.
(문서에 따라 "195곳"으로 적힌 곳이 있는데 초기 수집분 기준이다. 현재 DB 실측은 208곳.)
2026 한국관광공사 관광데이터 활용 공모전 제출을 겸하고 있어, 아래 제약이 설계에 영향을 준다.

## 반드시 지킬 것

**TourAPI 응답을 저장하지 않는다.** 한국관광공사 OpenAPI 응답은 매 요청 실시간으로 호출해야 하며,
DB나 서비스워커 캐시에 넣어 재사용하면 실격 위험이 있다. 반면 `holy_sites` 는 우리가 직접 수집한
자체 데이터이므로 얼마든지 저장·캐싱해도 된다. 이 구분을 흐리지 말 것.
자세한 배경은 `docs/20-architecture/adr/0002-tourapi-usage.md`.

**비밀 키를 클라이언트에 두지 않는다.** `VITE_` 접두사가 붙은 값은 브라우저 번들에 그대로 들어간다.
Gemini API 키처럼 감춰야 하는 값은 Supabase Edge Function 시크릿으로 넣고, 앱은 함수를 호출한다.

**AI 답변은 성지 DB 안으로 제한한다.** 종교 성지의 역사·전례 정보는 틀리면 신뢰가 바로 무너진다.
`supabase/functions/ai-guide` 는 질문과 관련된 성지 레코드를 컨텍스트로 넣고
"컨텍스트에 없으면 모른다고 답하라"를 시스템 프롬프트에 못 박아 두었다. 이 장치를 걷어내지 말 것.

**더미 데이터를 화면에 남기지 않는다.** 심사 대상이 "개발 완료된 완성 서비스"로 한정되므로,
가짜 핀·가짜 카운트·목업 카드는 실제 데이터로 바꾸거나 아예 빼고, 미구현 영역은
"준비 중"임을 사용자에게 정직하게 표시한다.

**코드는 GitHub 저장소가 기준이다.** OneDrive 아이디어 모음집 안의 앱 소스 사본과 zip 은
구버전이다. 거기서 코드를 꺼내 쓰지 말고, 작업 전에 `git pull` 부터 한다 (ADR 0005).

## 코드 구조 규칙

- 의존성 방향: `pages → features → shared`. 역방향 import 금지.
- DB 행(snake_case) ↔ 도메인 타입(camelCase) 변환은 각 feature 의 `api/*.repository.ts` 안에서만 한다.
  화면 컴포넌트가 `supabase` 클라이언트를 직접 부르지 않는다.
- 서버 상태는 TanStack Query 로 다루고, 캐시 키는 `shared/api/query-keys.ts` 한 곳에서 만든다.
- 라우트 경로 문자열은 `app/routes/paths.ts` 에서만 정의한다.
- 새 테이블을 추가하면 `supabase/migrations` 마이그레이션과 `shared/types/database.ts` 를 함께 고친다.

## 스타일

- 주석과 UI 문구는 한국어로 쓴다. "무엇을"보다 "왜"를 남긴다.
- 커밋 전 `npm run verify` (타입 검사 · 린트 · 테스트)를 통과시킨다.
- 접근성: 큰 글자 모드와 다국어 토글이 이미 있다. 새 화면도 이 설정을 존중해야 한다
  (고정 px 대신 rem 기반 Tailwind 크기 사용).

## 자주 보는 문서

| 알고 싶은 것                    | 문서                                            |
| ------------------------------- | ----------------------------------------------- |
| **지금 상태와 다음 할 일**      | `docs/이어서-할-일.md`                          |
| 무엇을 언제 만들 것인가         | `docs/10-product/로드맵.md`                     |
| 왜 이렇게 설계했는가            | `docs/20-architecture/`                         |
| 성지 취재·촬영은 어떻게 하는가  | `docs/30-content-ops/`                          |
| 도슨트 현장조사는 어떻게 하는가 | `docs/30-content-ops/도슨트-현장조사-매뉴얼.md` |
| 순교사·성지 배경지식            | `docs/40-knowledge/00-색인.md`                  |
