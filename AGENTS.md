# AGENTS.md

이 파일은 **OpenAI Codex 등 CLAUDE.md 를 읽지 않는 에이전트**를 위한 것이다.
사람과 Claude Code 는 `CLAUDE.md` 를 본다.

## 0. 먼저 읽을 것

**`CLAUDE.md` 를 읽고 그대로 따른다.** 프로젝트 규칙(TourAPI 저장 금지·비밀키 금지·
AI 답변 범위 제한·더미 데이터 금지·의존성 방향·한국어 주석)이 전부 거기 있다.
이 파일은 그 규칙을 대체하지 않고, **여러 에이전트가 같이 일할 때의 규칙만** 더한다.

## 1. 지금 이 저장소는 여러 에이전트가 함께 쓴다

Claude Code 창과 Codex 창이 **같은 폴더**를 본다. 브랜치가 같으므로 git 이
충돌을 알려주지 못한다. 한쪽이 저장하면 다른 쪽이 읽은 내용은 조용히 낡는다.

### 🔴 반드시 지킬 것

1. **지시서에 적힌 파일만 고친다.** 목록에 없는 파일은 오타여도 손대지 않는다.
   고쳐야 할 것을 발견하면 고치지 말고 작업기록에 적는다.
2. **커밋하지 않는다.** `git commit` · `git push` · `git checkout` · `git stash` 금지.
   파일만 고쳐두면 지시자(Claude Code 창)가 확인하고 커밋한다.
3. **작업 시작 전에 `git status` 를 찍어 작업기록에 남긴다.** 내가 만들지 않은
   수정 파일이 있으면 다른 에이전트가 일하는 중이다 — 그 파일은 피한다.
4. **`npm run verify` 는 돌려도 된다**(읽기 전용). 통과 여부를 작업기록에 적는다.

## 🔴 1-2. 절대 하지 않는 것 (2026-09-07 추가)

사장님이 매번 승인을 누르지 않아도 되도록 Codex 의 승인 프롬프트를 껐다
(`~/.codex/config.toml` — `sandbox_mode = "workspace-write"`).
**이제 아래 규칙이 유일한 안전장치다.** 하나라도 어기면 되돌리는 데 사람 손이 든다.

### 파일

- **지우지 않는다.** `Remove-Item` · `rm` · `del` 금지. 지워야 한다고 판단되면
  지우지 말고 작업기록에 "지워야 할 것"으로 적는다.
- **`.env.local` 을 읽지도 쓰지도 않는다.** DB 비밀번호와 API 키가 들어 있다.
  그 값은 어떤 파일·기록·출력에도 옮겨 적지 않는다.
- **`supabase/seed/` 와 `data/` 의 기존 데이터를 통째로 바꾸지 않는다.**
  지시서가 지목한 파일의 지목한 칸만 고친다.
- 파일을 새로 만들 때는 **지시서가 말한 경로에만** 만든다.

### git

- `git commit` · `git push` · `git checkout` · `git switch` · `git reset` ·
  `git restore` · `git stash` · `git clean` · `git rebase` · `git merge` **전부 금지.**
  읽기 명령(`git status` · `git diff` · `git log`)만 쓴다.
- 파일만 고쳐두면 지시자(Claude Code 창)가 확인하고 커밋한다.

### 명령

- **`npm install` · `npm ci` · 패키지 추가 금지.** 의존성이 필요하면 작업기록에 적는다.
- **DB 스크립트 금지** — `npm run db:migrate` · `db:status` · `admin:*` ·
  `photo:*` · `seed:*` · `translate:import` 는 운영 DB 를 건드린다. 돌리지 않는다.
- 돌려도 되는 것: `npm run verify` · `typecheck` · `lint` · `test` (읽기 전용).
- **네트워크가 막혀 있다.** 무언가를 내려받아야 하면 그 일은 하지 말고 작업기록에 적는다.

### 판단이 서지 않을 때

지시서 범위 안이면 스스로 판단해 끝까지 간다. 다만 **위 목록에 걸리는 일은
예외 없이 멈추고 작업기록에 적는다.** "이번만 괜찮다"는 판단을 하지 않는다.

## 2. 일은 지시서로 받는다

이 대화창 밖의 맥락은 모른다고 전제한다. 일은 언제나 파일로 온다:

```
docs/70-agent-workspace/tasks/<작업번호-이름>/지시서.md
```

지시서에는 목표·완료 조건·**범위 밖**·참고 파일 경로가 적혀 있다.
**완료 조건을 하나씩 확인하고, 무엇을 근거로 확인했는지** 같은 폴더의
`개발기록.md` 에 적는다("코드에 있으니 됐다"는 근거가 아니다 — 실제로 돌려본 것을 적는다).

막히면 멈추지 말고, 지시서 범위 안에서 판단해 끝까지 실행한다.
판단한 것과 그 이유를 작업기록에 남기면 된다.

## 3. 문체

주석·UI 문구·문서는 **한국어**로 쓴다. "무엇을"보다 "왜"를 남긴다.
사용자(사장님)는 비개발자다 — 기록도 사람이 읽을 수 있게 쓴다.
